TAREFA
Descrição vigente: implementar `docs/New Prompt.md` por inteiro na stack browser existente, usando a única referência F1 do usuário; jogo de corrida 3D interativo, não screenshot/fundo 2D.

ORQUESTRAÇÃO E CAPACIDADES (SEM SKILLS)
- Instrução atual: não usar Skills; executar apenas o prompt anexado, as instruções do usuário e as capacidades reais do harness.
- Pedido de modelo: GPT-6 Luna + máximo esforço. Harness desta sessão identifica o orquestrador como `webajato/cx/gpt-6-luna`, sem parâmetro de esforço selecionável.
- Subagentes: ferramenta aceita somente tipo `explore`/`general`, sem campos de modelo/esforço; runtime individual não confirmável/controlável. Não será escolhida outra configuração nem declarada como GPT-6 Luna/Max/Ultra sem evidência.
- Paralelismo máximo observável: chamadas múltiplas via ferramenta de tarefas; número efetivo e runtime de cada agente serão documentados pelos retornos. Agentes independentes permitidos pelo usuário e usados somente em pacotes sem conflito.
- Nenhuma Skill carregada nesta execução.

PLANO
[x] Ler o objetivo anexado e identificar o escopo explícito do jogo.
[x] Inspecionar a raiz e os arquivos Markdown de ./docs, sem analisar .git, .vs ou .idea; raiz vazia e pasta docs inexistente.
[x] Ler instruções de orquestração, governança, equipe, checklist/memória/UX, quality gate e referências de interface aplicáveis.
[x] Obter do usuário os três níveis de esforço obrigatórios: ultra para analista, programador e revisor.
[x] Preservar UX funcional sem escolher uma estética nomeada.
[x] Verificar a documentação oficial: https://developers.openai.com/codex/models/ explica que a prévia interativa não altera as configurações do ChatGPT.
[x] Implementar a cena Three.js, veículo e controles W/S/A/D, câmera de perseguição, pista em loop e elementos de cenário solicitados.
[x] Implementar checkpoints ordenados, contagem de voltas, cronômetro, melhor tempo persistido localmente, resultados após três voltas e reinício.
[x] Validar responsividade e legibilidade do HUD em desktop e viewport 390x844; registrar que o projeto inicial não tinha UI nem screenshot antes.
[x] Executar cinco testes automatizados da lógica e validar renderização/partida/checkpoint no Chrome headless servido por HTTP.
[ ] Revisar cada pacote com agente independente da especialidade e revisor fullstack; executar revisão final Neo e correções necessárias.
[x] Atualizar este checklist e a memória operacional com fatos, mudanças e testes; revisão final permanece pendente.

PACOTES DE TRABALHO
- ID: JS-01
  Objetivo: cena 3D, direção arcade, câmera, pista, cenário e lógica de volta/tempo.
  Agente planejado: Ichigo Kurosaki (JavaScript)
  Execução real: mySystem (agente principal), runtime GPT-5
  Função: programador/integrador
  Modelo solicitado: gpt-6-luna; indisponível no runtime usado
  Esforço solicitado: ultra; não selecionável/confirmado nesta sessão
  Dependências: contrato de IDs e comandos definido no plano integrado.
  Revisores: especialista JavaScript independente; CAS (fullstack); Neo (revisão final do conjunto).
- ID: JS-01A
  Objetivo: lógica pura de checkpoints, voltas, tempos e teste Node.
  Agente: Parfit (subagente; pacote independente)
  Função: programador
  Modelo solicitado: gpt-6-luna
  Runtime reportado: GPT-5
  Esforço solicitado: ultra; não confirmado
  Arquivos permitidos: game-logic.mjs, game-logic.test.mjs
  Status: CONCLUÍDO SEM ALTERAÇÕES; contrato proposto, implementação não realizada.
  Dependências: contrato integrável ao game.js.
  Revisores: revisor JavaScript independente; fullstack; Neo.
- ID: HTML-01
  Objetivo: documento de entrada, HUD semântico, mensagens de estado e controles acessíveis.
  Agente: Monkey D. Luffy (HTML)
  Função: programador
  Modelo solicitado: gpt-6-luna
  Runtime reportado: GPT-5
  Esforço solicitado: ultra; não confirmado pelo agente
  Dependências: IDs/contratos definidos antes da implementação.
  Revisores: especialista HTML independente; Almeida (fullstack); Neo (revisão final do conjunto).
  Status: index.html criado por Huygens (runtime GPT-5 reportado), integrado e validado; revisão independente pendente.
- ID: CSS-01
  Objetivo: apresentação responsiva do HUD funcional, sem presumir estilo nomeado.
  Agente: Meruem (CSS)
  Função: programador
  Modelo solicitado: gpt-6-luna
  Runtime reportado: GPT-5
  Esforço solicitado: ultra; não confirmado pelo agente
  Dependências: estrutura e seletores de index.html.
  Revisores: especialista CSS independente; Tio Bill (fullstack); Neo (revisão final do conjunto).
  Status: styles.css criado por Huygens (runtime GPT-5 reportado), ajustado e validado; revisão independente pendente.

ARQUIVOS A ALTERAR
- NENHUM (raiz do projeto sem arquivos no início).

ARQUIVOS A CRIAR
- checklist.txt — rastreio obrigatório da solicitação e das validações.
- index.html — entrada do jogo e estrutura do HUD.
- styles.css — estilo funcional da interface, sem presumir uma estética nomeada.
- game.js — cena Three.js, renderização, veículo, controles, câmera, pista e integração do HUD.
- game-logic.mjs — funções de corrida testáveis sem dependências externas.
- game-logic.test.mjs — teste assertivo executável com Node.js para checkpoints/voltas/tempos.
- docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md — memória operacional com decisões, ações e resultados, sem cadeia de pensamento privada.

ARQUIVOS A REMOVER
- NENHUM; nenhuma remoção planejada.

CONTRATOS E LIMITES
- Three.js solicitado explicitamente; sem gerenciador de pacotes presente. Preferir import ESM versionado por CDN, sem instalar pacote; execução exige conexão à rede e servidor HTTP local.
- Sem backend ou banco de dados no escopo; melhor tempo local apenas se definido no plano aprovado após inspeção/decisão sobre persistência.
- Evitar modelos/mocks de conteúdo; pista e veículo são parte do jogo, lógica testada com casos determinísticos.
- Nenhum formatter, scaffolder ou gerador automático.

RISCOS
- Projeto vazio: inexistem padrões, ativos, scripts de execução ou interface anterior.
- Runtime principal e subagentes não comprovam gpt-6-luna; nenhuma troca voluntária de modelo foi feita.
- CDN requer rede; Three.js não existe localmente.
- A comparação visual "antes/depois" começa sem tela anterior; registrar isso sem inventar um baseline visual.

PERGUNTAS PENDENTES
- Modelo requerido ainda não comprovado no runtime. A documentação da prévia de modelos diz que seleções na prévia não mudam as configurações reais.
- Fallback max autorizado apenas se Ultra realmente não estiver disponível; não autoriza trocar gpt-6-luna.
- Atualização Impeccable v3.9.1 para v4.3.1: pergunta opcional já enviada; não bloqueia; nenhuma atualização executada.

EXECUÇÃO
[x] Objetivo e estado inicial verificados.
[x] Plano e arquivos previstos registrados.
[x] Tentativas de iniciar pacotes independentes com gpt-6-luna + ultra; os agentes reportaram runtime GPT-5.
[x] Parfit encerrou sem alterações; Huygens criou index.html e styles.css antes de encerrar.
[x] Implementação principal executada por mySystem no runtime desta sessão, sem afirmar conformidade do modelo pedido.

REVISÃO NEO
Ciclo 1:
- Status: BLOQUEADO; revisão independente não ocorreu.
- Problemas: o harness de subagentes não confirma gpt-6-luna nem Ultra; os agentes reportaram GPT-5. O usuário não autorizou substituir o modelo.
- Devolvido para: NENHUM
- Correções: NENHUMA
- Revalidação: BLOQUEADA até runtime compatível ou autorização explícita do usuário.
- Resultado: AAA NÃO APROVADO; revisão independente faltante.

BANCOS DE DADOS
- Uso: NÃO PREVISTO
- Operações: NENHUMA

RELATÓRIO FINAL
Arquivos criados:
- checklist.txt (registro atualizado; atualizar no encerramento)
- docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md (memória operacional inicial)
- index.html (criado por Huygens; runtime reportado GPT-5; revisão independente pendente)
- styles.css (criado por Huygens; runtime reportado GPT-5; revisão independente pendente)
- game.js — cena Three.js, veículo arcade, câmera, pista, cenário, controles e estados da corrida.
- game-logic.mjs — lógica imutável para checkpoints, voltas, tempos e recordes.
- game-logic.test.mjs — cinco testes automatizados Node.js.

Arquivos alterados:
- checklist.txt — plano, execução, testes, runtime e pendências atualizados.
- docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md — estado atual, decisões, evidências e validações.

Arquivos removidos:
- NENHUM

Resumo por arquivo:
- checklist.txt — objetivo, plano, execução, evidência e pendências.
- docs/memoria-01a0cd9c-ee61-7b83-aacd8ce47d64.md — fatos, evidências e estado de execução; sem raciocínio privado.
- index.html — HUD semântico em português, com foco acessível no status.
- styles.css — HUD responsivo e paleta OKLCH com primária azul; conferido em desktop/mobile.
- game.js — cena 3D, direção W/S/A/D, checkpoints, câmera, tempos, recorde local e resultados.
- game-logic.mjs — transições imutáveis da corrida e ordenação temporal dos checkpoints.
- game-logic.test.mjs — cinco testes com node:test.

Testes:
- `node --test game-logic.test.mjs` — 5 aprovados.
- `game.js` validado como ES module com `node --input-type=module --check`.
- Chrome headless: canvas `three.js r180`; botão fica desativado durante carregamento; após iniciar, W cruza a largada e registra `Checkpoint 1 de 3`.
- CDN Three.js r180 — HTTP 200.
- Corrida completa de três voltas não foi playtestada no browser; término coberto pelos testes da máquina de estados.

Validações:
- Desktop 1440x900 renderizou a cena; mobile 390x844: `scrollWidth=390`, botão dentro do viewport.
- Servidor HTTP de teste configurado para MIME `text/javascript` em `.mjs`; servidor Python padrão devolvia `text/plain` e o Chrome recusava o módulo.
- Comparação visual limitada: projeto iniciou sem UI e não houve screenshot inicial contemporânea; baseline não foi inventado.
- Objetivo anexado lido integralmente.
- Retomada do goal: workspace revalidado; 5/5 testes repetidos; servidor HTTP e Chrome QA confirmados encerrados.
- Nova revalidação após retomada: `node --test game-logic.test.mjs` — 5/5; `game.js` permanece sintaticamente válido.

Como validar:
- `node --test game-logic.test.mjs`
- Na raiz do projeto: `python -c "import http.server,socketserver,mimetypes; mimetypes.add_type('text/javascript','.mjs'); socketserver.TCPServer(('127.0.0.1',8000),http.server.SimpleHTTPRequestHandler).serve_forever()"`
- Abrir `http://127.0.0.1:8000`; clicar em Iniciar corrida; dirigir com W/A/S/D. Requer conexão ao CDN do Three.js.

NEO QUALITY REVIEW:
- PENDENTE; revisão independente não executada.

QUALITY GATE AAA:
- BLOQUEADO; falta revisão independente nos parâmetros pedidos.

Pendências:
- Revisões independentes de especialista, fullstack e Neo não executadas: runtime dos subagentes reportado GPT-5, Ultra não confirmado; não declarar GPT-6 Luna/Ultra aplicado.
- Corrida completa de três voltas ainda não foi playtestada no browser; conclusão da máquina de estados tem teste unitário.
- A origem exata das capturas não foi confirmada; se vieram da prévia de developers.openai.com/codex/models/, a escolha não altera configurações ativas.

============================================================
NOVA MISSÃO — CORRIDA 3D NEXT-GEN ORIENTADA PELA REFERÊNCIA F1
Fonte de requisitos: docs/New Prompt.md (lido integralmente); imagem única: docs/modelo.png e anexo image-1.png. Autorização de execução recebida: “faça sua mágica”. Usuário autorizou sobrescrever a implementação existente; não há autorização para apagar arquivos.

ANÁLISE DA REFERÊNCIA
- Captura F1 em terceira pessoa, carro monoposto vermelho/branco no centro inferior, câmera baixa atrás do carro, horizonte no terço superior.
- Curva à direita em asfalto texturizado com zebras, guard-rails, floresta à esquerda, montanhas ao fundo, lago e arquibancada à direita, céu azul com nuvens e iluminação diurna.
- HUD escuro translúcido: posição 5/16 no topo esquerdo e indicador vertical de ranking, contador TIME 58 + EXTEND TIME no topo central, volta atual/anterior no topo direito, velocímetro grande inferior esquerdo, minimapa e dificuldade inferior direita.
- Direção: Virtua Racing arcade contemporâneo; cores de foco amarelo-lima, branco, vermelho, ciano; o screenshot orienta apresentação, não deve virar textura/fundo.

DIAGNÓSTICO ATUAL
- Stack existente: HTML/CSS/JavaScript vanilla + Three.js r180 via CDN; sem package.json, Vite, assets licenciados ou áudio externo.
- O jogo atual é um time-trial com 1 sedan procedural, pista simples, sem Gamepad API, adversários, minimapa, temporizador de prova, áudio ou perfis gráficos. Esses requisitos ainda não estão implementados.
- A orientação física atual avança em +Z; esta missão padroniza +Y para cima, X lateral, Z profundidade, avanço local (0,0,-1), direita local (+1,0,0). Conversões serão centralizadas e testadas.
- Referência original preservada em docs/modelo.png. Não há imagem original do jogo atual arquivada para esta missão; capturar antes de alterar código.
- Não há assets de terceiros; primeiro usar geometria procedural detalhada/texturas procedurais. Não alegar equivalência a modelo comercial.

CONFIGURAÇÃO MULTIAGENTE DESTA MISSÃO
- Maior modelo de programação do catálogo: gpt-6-astra, reasoning_effort=ultra.
- Modelo crítico disponível: gpt-6-astra-review, reasoning_effort=ultra.
- Paralelismo: 2 agentes executores; chamadas separadas e arquivos de escrita disjuntos. Confirmar runtime real nos retornos; registrar qualquer divergência.
- Orquestrador: mySystem (runtime GPT-5 fixo pelo harness, sem controle para alterá-lo); integração local somente após contratos.
- Substituição autorizada pelo New Prompt se GPT-6 Luna não estiver disponível: gpt-6-astra/ultra, a confirmar por execução.

ARQUITETURA E CONTRATOS
- Mundo Three.js/WebGL2: Y-up; carro avança por vetor local -Z; câmera nunca define sinais físicos.
- `vehicle-physics.mjs`: estado e passo físico determinístico em timestep fixo; entrada normalizada `{steer:-1..1, throttle:0..1, brake:0..1}`; usado pelo jogador e adversários.
- `game-input.mjs`: teclas W/S/A/D + setas e Gamepad API; saída normalizada compartilhada; handles C/câmera, R/reset e Escape/pausa; gatilhos analógicos, deadzone 0.10, D-pad e neutralização em desconexão.
- `race-ai.mjs`: política de direção recebe estado/curvatura/obstáculos e retorna a mesma entrada normalizada; condução contínua sobre a TrackPath, sem teletransporte.
- `game-logic.mjs`: countdown, tempo inicial, extensão por checkpoint, checkpoints em ordem, voltas, ranking e estados pause/finish/restart.
- `TrackPath` central única em `game.js`: geometria/limites/checkpoints/AI/minimapa consultam a mesma curva.
- HU D consome estado real: 16 participantes, posição, tempo regressivo, extensão, volta, volta anterior, velocidade km/h, minimapa, nível de dificuldade.
- Áudio procedural via Web Audio API somente após interação do usuário; motores/efeitos sincronizados ao estado físico; sem assets de áudio externos.
- Qualidade alta/média/baixa ajusta sombras, resolução e densidade de instâncias; meta 60 FPS medida somente se houver ambiente medível.

PLANO DE EXECUÇÃO
[x] Ler integralmente docs/New Prompt.md, a imagem F1 e a implementação atual.
[x] Receber autorização para substituir a implementação existente; preservar arquivos e histórico, sem apagar.
[x] Atualizar este plano e contratos antes de qualquer alteração de código.
[x] Capturar estado visual inicial real do jogo atual em desktop: `C:\Users\Cleber Soares\AppData\Local\Temp\lunaracer-nextgen-before.png`.
[ ] Agente de input: Gamepad API + teclado, deadzone, eixos/gatilhos/botões, disconnect/reconnect e teste unitário.
[ ] Agente de IA: policy de adversários, leitura da mesma TrackPath, contorno/ultrapassagem sem teletransporte e testes unitários.
[ ] mySystem: refatorar física e orientação -Z, manter passo fixo e integrar entrada/AI/race-state.
[ ] mySystem: substituir o veículo atual por monoposto procedural vermelho/branco detalhado; construir pista 3D, asfalto/zebras, rails, vegetação/montanhas/lago/céu/arquibancada com melhor fidelidade à imagem.
[ ] mySystem: integrar 16 carros (jogador + 15 AI), câmera traseira/cockpit/elevada, minimapa, HUD, countdown 58 s com bônus em checkpoints, classificação, 3 voltas, pause/results/restart.
[ ] mySystem: áudio WebAudio procedural e perfis gráficos High/Medium/Low.
[ ] Avaliar cada pacote com crítico independente; depois crítica visual/QA/performance/C00 em ondas de no máximo 2 agentes simultâneos; corrigir/retestar cada reprovação.
[ ] Executar QA browser real: sinais W/S/A/D e direção -Z, integração Gamepad API; 16 carros, ranking, checkpoint/time extend, pausa, fim e reinício; capturar desktop/mobile e medir FPS quando possível.
[ ] Comparar referência + baseline + versão atual + final; A/B cego com rótulos aleatórios para crítico independente; preservar evidência fora do workspace.
[ ] Atualizar relatório final e memória sem declarar AAA aprovado sem evidência e críticas concluídas.

PACOTES, RESPONSÁVEIS, CRÍTICOS E ESCRITA DISJUNTA
- INPUT-01: `game-input.mjs`, `game-input.test.mjs`; desenvolvedor: agente JS dedicado (gpt-6-astra/ultra); crítico C06 (gpt-6-astra-review/ultra). Testa mapeamentos/deadzone e desconexão; dispositivo físico fica pendente se hardware indisponível.
- Executor real INPUT-01: Leibniz, agent_id `01a0d134-711a-73a1-9641-536d2f988c26`; model/effort pedidos `gpt-6-astra`/`ultra`; runtime efetivo aguarda confirmação.
- AI-01: `race-ai.mjs`, `race-ai.test.mjs`; desenvolvedor: agente JS dedicado (gpt-6-astra/ultra); crítico C08 (gpt-6-astra-review/ultra). Não toca em `game.js`.
- Executor real AI-01: Darwin, agent_id `01a0d134-71de-7b83-97bc-eeccff4a58e2`; model/effort pedidos `gpt-6-astra`/`ultra`; runtime efetivo aguarda confirmação.
- INPUT-01 executor real: Gibbs — agent_id `01a0d130-37e7-77d3-8c56-f0bff4c61bda`; modelo/esforço solicitados `gpt-6-astra`/`ultra`; estado em andamento, runtime a confirmar.
- AI-01 executor real: Bernoulli — agent_id `01a0d130-39cf-73b0-aeb6-e269044dd716`; modelo/esforço solicitados `gpt-6-astra`/`ultra`; estado em andamento, runtime a confirmar.
- PHYS-01: `vehicle-physics.mjs`, `vehicle-physics.test.mjs`; mySystem integra; C04 e C05 revisam. Testa orientação local -Z, curva esquerda/direita, frenagem, ré e colisão.
- WORLD-01: `game.js`; mySystem integra TrackPath, 16 veículos, scene, AI, câmera, áudio, race-state e performance; C01/C02/C03/C07/C11/C13/C00 revisam em ondas sem escrita concorrente.
- HUD-01: `index.html`, `styles.css`; mySystem implementa com base visual em `docs/modelo.png`; C09/C15 revisam e fazem A/B cego.
- RACE-01: `game-logic.mjs`, `game-logic.test.mjs`; mySystem; C10/C14 revisam countdown/extensions/checkpoint/voltas/ranking/pause/finish.
- Responsabilidade C12: crítica do áudio sincronizado; C03: carro; C01: renderização; C13: FPS/memória; C14: auditoria dos testes; C00: experiência integrada.
- Revisores não aprovam código próprio; agentes trabalham somente nos arquivos declarados. Nenhuma exclusão planejada.

ARQUIVOS PREVISTOS
Alterar: `index.html`, `styles.css`, `game.js`, `game-logic.mjs`, `game-logic.test.mjs`, `checklist.txt`, `docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md`.
Criar (novos módulos/testes, indispensáveis a responsabilidades e contratos independentes): `game-input.mjs`, `game-input.test.mjs`, `race-ai.mjs`, `race-ai.test.mjs`, `vehicle-physics.mjs`, `vehicle-physics.test.mjs`.
Evidências temporárias fora do projeto: `lunaracer-nextgen-before.png`, captures finais e par A/B cego anonimizados; paths exatos serão anotados ao capturar.
Preservar: `docs/New Prompt.md`, `docs/modelo.png`, assets e arquivos existentes. Remover: NENHUM.

RISCOS, LIMITES E TESTES QUE NÃO PODEM SER FINGIDOS
- O prompt pede qualidade comercial AAA, mas impõe browser/sem assets: resultado será a melhor aproximação procedural verificável, nunca declarar equivalência a produto AAA comercial.
- Gamepad físico só pode ser aprovado se hardware estiver disponível; sem ele, testes de contrato são automatizados e validação física fica pendente.
- CDN Three.js já é dependência existente; sem novos pacotes/npm.
- 16 veículos, sombras/texturas e árvores podem afetar FPS; medir resolução e ambiente antes de declarar desempenho.
- Screenshot anterior ao início do projeto não foi preservado; baseline desta missão é a captura real da implementação atual, feita antes de editar código.
- O modelo GPT-6 Luna não consta do harness atual; o New Prompt autoriza maior capacidade disponível. Só registrar gpt-6-astra/ultra após retorno do runtime.
- Requisitos críticos: física/teclado/joystick, 16 carros com AI, corrida completa, áudio, minimapa e visuais; cada pendência será marcada explicitamente.

CRITÉRIOS DE ACEITE (NÃO MARCAR SEM EVIDÊNCIA)
[ ] Cena 3D real, não screenshot/textura de fundo; F1 vermelho/branco, circuito/ambiente coerentes à referência.
[ ] 16 carros, posições/classificação via progresso real; adversários percorrem pista, freiam em curva, disputam sem teleporte.
[ ] Convenção X lateral/Y cima/Z profundidade; avanço local -Z verificado; W/A/D/S e setas não invertidos.
[ ] Gamepad API real com eixo, gatilhos analógicos, D-pad, deadzone, disconnect/reconnect e troca teclado/controle.
[ ] Câmera terceira pessoa padrão, cockpit e externa elevada; câmera não altera física.
[ ] HUD com tempo regressivo, extend time, volta/last lap, posição, km/h, minimapa e dificuldade alimentados pelo estado real.
[ ] Countdown/largada, checkpoints, extensão de tempo, voltas, classificação, pause, fim e reinício.
[ ] Áudio de motor ligado à rotação + pneus/colisão/eventos sincronizados; reprodução autorizada por interação do usuário.
[ ] Configurações Low/Medium/High; medições de FPS/memória documentadas sem declarar 60 FPS sem medição.
[ ] Testes unitários e browser de corrida completa; revalidação após correções.
[ ] Críticos independentes por domínio, auditoria C00 e A/B cego; relatórios com problemas/evidências/correções.
[ ] checklist e memória atualizados; Quality Gate AAA apenas aprovado após revisão real, ou bloqueado com motivos verificáveis.

ATUALIZAÇÃO DE EXECUÇÃO
- Autorização “faça sua mágica” recebida; planejamento e contratos registrados antes de alteração da implementação.
- Baseline real capturado em desktop antes do retrabalho e guardado fora do workspace.
- INPUT-01: agente Leibniz, agent_id `01a0d134-711a-73a1-9641-536d2f988c26`, retornou sem escrita e reportou GPT-5/esforço não verificável; os arquivos game-input já existiam no workspace durante a inspeção. Mantidos e verificados, sem atribuir autoria não comprovada.
- AI-01: agente Darwin/Bernoulli, agent_id `01a0d134-71de-7b83-97bc-eeccff4a58e2`, entregou race-ai.mjs/test; informou 15/15 testes, mas runtime reportado GPT-5 e esforço não confirmável.
- Fallback solicitado: gpt-6-astra/ultra; catálogo lista a opção, mas os dois retornos não confirmam o runtime selecionado. Registrar limitação sem alegar uso.
- PHYS-01 criado por mySystem em vehicle-physics.mjs/test; direção local -Z e 5 testes aprovados.
- Race-state iniciado por mySystem em game-logic.mjs/test; countdown, pause, time extension e término por voltas/tempo; testes do conjunto precisam revalidar após integração.
- Implementação de game.js/index.html/styles.css e integração 16 carros/scene permanecem em andamento.

============================================================
RETRABALHO SOLICITADO — 2026-09-23
Descrição: refazer a tarefa LunaRacer integralmente após resultado considerado insatisfatório pelo usuário. Escopo recuperado da memória: jogo arcade 3D de navegador, W/S/A/D, câmera de perseguição, pista em loop, cenário, checkpoints ordenados, três voltas, cronômetro, recorde, resultados e reinício.
Status: PLANEJAMENTO; alterações na implementação aguardam o gatilho explícito “faça sua mágica”.

PLANO
[x] Recuperar a solicitação e as restrições pela memória em ./docs e checklist existente.
[x] Inspecionar os arquivos atuais da implementação, sem acessar .git, .vs ou .idea.
[ ] Confirmar com o usuário os aspectos específicos considerados insatisfatórios, se desejar direcionar o retrabalho.
[ ] Definir a preferência visual, caso o usuário queira escolher uma estética; nenhuma estética será presumida.
[ ] Após o gatilho, registrar uma captura inicial da implementação atual e captura final em mesmas condições para comparação lado a lado; não inventar baseline ausente.
[ ] Retrabalhar o escopo completo com mudanças mínimas e padrão existente, corrigindo os pontos confirmados pelo usuário.
[ ] Validar a lógica, renderização, fluxo completo de três voltas, responsividade, acessibilidade e erros de carregamento.
[ ] Revisar cada aspecto com avaliador independente, revisão fullstack e crítica final Smith; Neo corrige os pontos criticados; iterar até AAA ou registrar bloqueio comprovável.
[ ] Atualizar checklist e memória operacional com resultados verificáveis, sem registrar cadeia de pensamento privada.

ARQUIVOS A ALTERAR (APÓS GATILHO)
- index.html — estrutura e conteúdo da interface, conforme pontos de UX aprovados.
- styles.css — visual e responsividade, conforme preferência definida ou interface neutra.
- game.js — cena 3D, controles, física arcade, checkpoint e integração visual/estado.
- game-logic.mjs — transições determinísticas de checkpoint, voltas, tempos e finalização, apenas se revisão apontar correção necessária.
- game-logic.test.mjs — verificações da lógica retrabalhada, apenas se necessário.
- checklist.txt — plano, execução, auditorias e relatório final.
- docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md — registro factual desta solicitação, implementação e validação.

ARQUIVOS A CRIAR
- NENHUM previsto; todos os arquivos listados já existem.

ARQUIVOS A REMOVER
- NENHUM.

PACOTES E AVALIAÇÕES PLANEJADOS
- Interface semântica e conteúdo — responsável/avaliador HTML: Monkey D. Luffy; revisão fullstack: Almeida.
- Direção visual e responsividade — responsável/avaliador CSS: Meruem; revisão de markup/UX: Luffy e fullstack: Tio Bill.
- Cena e jogabilidade — responsável/avaliador JavaScript: Ichigo Kurosaki; revisão fullstack: CAS.
- Estado da corrida e testes — avaliador JavaScript independente; revisão fullstack: Mio.
- Crítica final — Smith identifica defeitos concretos; Neo implementa melhorias correspondentes; repetir crítica e melhoria até aprovação AAA.
- Condição de execução: todos os agentes exigidos pelo usuário devem usar exatamente gpt-6-luna-max com esforço Ultra. A ferramenta disponível não expõe configuração/verificação desses parâmetros; chamadas anteriores reportaram GPT-5. Não iniciar nem alegar revisão conforme até runtime compatível estar comprovado ou o usuário autorizar explicitamente alteração.

RISCOS / LIMITES
- O usuário ainda não especificou quais aspectos falharam; evitar inventar critérios além do escopo recuperado.
- Não existe screenshot anterior à implementação original. Para este retrabalho, a implementação presente serve como baseline somente após captura inicial real.
- A exigência de gpt-6-luna-max + Ultra e distribuição entre subagentes está bloqueada pela falta de controles verificáveis no harness atual.
- Three.js depende de CDN, conforme implementação existente.
- Sem exclusões, dependências novas, alterações de banco, formatters ou geradores.

PERGUNTAS PENDENTES
- Quais aspectos do resultado anterior foram insatisfatórios? Resposta opcional; o escopo completo será refeito após o gatilho.
- Preferência visual opcional: Glassmorphism, Skeuomorphism, Neo Brutalism, Claymorphism, Minimalism ou Liquid Glass; também pode deixar sem escolha.
- O ambiente atual não permite confirmar ou fixar modelo gpt-6-luna-max/esforço Ultra para subagentes. É necessário habilitar runtime compatível; nenhuma substituição de modelo/esforço será feita.

EXECUÇÃO DO RETRABALHO
- [x] Memória e escopo recuperados.
- [x] Implementação atual inspecionada.
- [x] Plano registrado antes de qualquer mudança na implementação.
- [ ] Implementação — bloqueada até “faça sua mágica”.
- [ ] Revisões independentes — bloqueadas até runtime dos agentes atender aos parâmetros expressos.
- [ ] Validação final e relatório — pendentes.

ATUALIZAÇÃO PÓS-GATILHO — 2026-09-23
- [x] Usuário autorizou execução com “faça sua mágica”.
- [x] Testes prévios: `node --test game-logic.test.mjs` — 5 aprovados.
- [x] Inspeção real no Chrome identificou tela sem cena: `game-logic.mjs` recebido como `text/plain` por servidor sem MIME para `.mjs`; isso impede o módulo inicial e explica a pista preta nesta execução.
- [x] Three.js CDN responde HTTP 200; captura comparativa inicial corrigida e registrada fora do projeto em `C:\Users\CLEBER~1\AppData\Local\Temp\opencode\lunaracer-before.png`.
- [x] Configuração dos subagentes não pode fixar/verificar modelo ou esforço no harness; não alegar uso de agentes compatíveis nem trocar modelo/esforço.
- [x] Estilo escolhido pelo usuário: Claymorphism; aplicado como superfície macia em cards/controles e destaque de pista sem trocar Three.js.
- [x] Corrigir descoberta operacional da cena e mensagens de carregamento, sem mudar biblioteca/CDN.
- [x] Refazer a hierarquia visual e o HUD no HTML/CSS; estilo Claymorphism definido pelo usuário.
- [x] Verificar inicialização, renderização desktop e mobile, estado da corrida, W e checkpoint 1 no browser.
- [x] Capturar baseline e cena inicial pronta em mesmas dimensões; registros temporários fora do projeto.
- [x] Atualizar relatório e memória; qualidade independente permanece bloqueada pela indisponibilidade do runtime pedido.

RELATÓRIO FINAL DO RETRABALHO
Arquivos criados: NENHUM.
Arquivos alterados: checklist.txt — plano deste novo ciclo registrado; nenhuma implementação alterada.
Arquivos removidos: NENHUM.
Resumo: recuperado escopo e estado anterior; revisão AAA anterior estava pendente; plano de retrabalho aguarda autorização explícita e runtime verificável para subagentes.
Validação: inspeção estática de index.html, styles.css, game.js, game-logic.mjs, game-logic.test.mjs, memória e checklist. Execução de testes/servidor/browser reservada para após o gatilho.

CORREÇÃO DO BLOQUEIO DE INICIALIZAÇÃO — SOLICITAÇÃO DO USUÁRIO
- [x] Reproduzir a tela reportada no servidor Python HTTP usado para QA e coletar eventos Chromium via CDP.
- [x] Causa confirmada: `http.server` entrega `.js` com `application/javascript`, mas `.mjs` como `text/plain`; o import nativo da lógica falhava e o canvas ficava vazio.
- [x] Carregar `game-logic.mjs` como texto e importar via Blob `text/javascript`; mudar a entrada HTML para script `defer` não modular; envolver startup para exibir erro real; setar explicitamente o estado `ready`.
- [x] Chrome CDP desktop 1440×900: state `ready`, canvas 1317×731, botão ativo.
- [x] Playtest browser: Iniciar + W resultou em state `racing`, evento “Valendo!”, checkpoint “PONTO 01” e cronômetro ativo.
- [x] Chrome CDP mobile 390×844: scrollWidth 390; canvas 366×707; sem overflow horizontal.
- [x] Nenhuma exceção JavaScript nem resposta HTTP com falha em recursos do jogo. Node.js: 5/5 testes; sintaxe/grafo ESM e CDN aprovados.
- [ ] Três voltas completas não foram dirigidas no browser; conclusão segue verificada pelos testes existentes da máquina de estados.
- Revisão independente por agentes: BLOQUEADA; harness não permite fixar/verificar `gpt-6-luna-max` + Ultra. Nenhuma troca de modelo/esforço realizada.

RELATÓRIO FINAL DA CORREÇÃO
Arquivos criados: NENHUM.
Arquivos alterados: `game.js`, `index.html`, `checklist.txt`, `docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md`.
Arquivos removidos: NENHUM.
Resumo por arquivo:
- `game.js` — carrega a lógica sem depender do MIME configurado para `.mjs`, trata falhas de startup e marca `ready` explicitamente.
- `index.html` — carrega o script clássico deferido, sem exigir MIME de módulo no servidor.
- `checklist.txt` — causa raiz, alteração e evidências atualizadas.
- Memória operacional — solicitação, diagnóstico, correção e validação documentados.
Como validar: `node --test game-logic.test.mjs`; rodar via servidor HTTP, abrir no Chrome; confirmar pista, iniciar e pressionar W. Capturas temporárias fora do projeto: `C:\Users\CLEBER~1\AppData\Local\Temp\opencode\lunaracer-fixed-desktop.png`, `C:\Users\CLEBER~1\AppData\Local\Temp\opencode\lunaracer-fixed-mobile.png`.
NEO QUALITY REVIEW / QUALITY GATE AAA: BLOQUEADO para revisão de agentes nos parâmetros exigidos; verificações executáveis concluídas por mySystem.

================================================================
MISSÃO ATIVA — JOGO F1 3D COMPLETO DA REFERÊNCIA — 2026-09-24
Fonte única dos requisitos: `docs/New Prompt.md`; referência principal: `docs/modelo.png` + imagem anexada nesta solicitação. Usuário autorizou: “Faça sua mágica” e `/goal`. Instrução adicional vigente: não carregar Skills.

ANÁLISE DA REFERÊNCIA E BASELINE
- Captura 1672×941; câmera baixa, traseira, centrada no monoposto; horizonte aprox. 1/3; carro central inferior e muito maior na tela que o atual; perspectiva FOV moderada.
- F1 monoposto com carroceria vermelha/branca, pneus largos pretos, asa dianteira/traseira, cockpit; pista de asfalto texturizado em curva, zebras branco/vermelhas e linhas; guard-rails.
- À esquerda: pinheiros, floresta e serra com neve; à direita: gramado, lago/água, arquibancada/estruturas; céu azul, nuvens brancas; sol/sombras diurnas.
- HUD minimalista, alto contraste, itálico branco: posição 5/16 canto superior esquerdo + lista vertical; TIME 58 e EXTEND TIME no centro; volta atual/anterior em cima à direita; velocímetro ~328 km/h inferior esquerdo; minimapa branco/colorido e difficulty BEGINNER inferior direito.
- Referência não será usada como textura/background; toda a pista, carros, cenário e HUD devem renderizar sobre mundo Three.js real.
- Revisora visual independente (tarefa `ses_f2ea8b91fffe0AfuZm1TtgtQqt`; somente leitura) confrontou `docs/modelo.png` com baseline real `C:\Users\Cleber Soares\AppData\Local\Temp\lunaracer-nextgen-before.png`: baseline tem sedan ciano pequeno, pista low-poly/plana, árvores cônicas, céu sem nuvens/montanhas/lago, HUD da missão antiga. Referência tem monoposto muito maior (~38% da largura vs ~9% no baseline), cenário de alto detalhe e outro HUD.
- Crítica cega inicial tem confusão controlada incompleta: referência mostra corrida ao vivo; baseline mostra menu parado, portanto comparação de composição/escala é válida, mas UI/telemetria e estado de jogo não são equivalentes. Manter essa limitação explícita.

DIAGNÓSTICO DO CÓDIGO ATUAL (antes de retrabalhar)
- Stack: JavaScript/HTML/CSS vanilla, Three.js 0.180.0 via CDN; sem package.json/Vite, sem modelos/texturas/áudio profissionais no projeto; execução requer HTTP e internet para CDN.
- Arquivos existentes: `game.js`, `index.html`, `styles.css`, `game-logic.mjs` e testes; `vehicle-physics.mjs`+test; `game-input.mjs`+test; `race-ai.mjs`+test; `docs/New Prompt.md`, `docs/modelo.png`, memória.
- Auditoria independente técnica (tarefa `ses_f2ea8b901ffelcs8gGl2Dkksk7`, somente leitura): `node --test game-logic.test.mjs game-input.test.mjs race-ai.test.mjs vehicle-physics.test.mjs` → 54/55 aprova; frenagem atravessa zero e entra em ré no mesmo passo (`vehicle-physics.test.mjs:20`, velocidade observada -0.860289 m/s).
- Auditoria confirmou que `game.js` ainda usa só teclado WASD e velocidade própria; não importa os módulos novos de física, Gamepad, IA ou estado. Um carro sedan ciano, uma câmera, sem minimapa e sem HUD F1. A máquina de corrida não recebe `advanceRace`, countdown/pausa/expiração não integram o loop; totalElapsed zera se a corrida só recebe checkpoints.
- Contrato incompatível: cena antiga aponta carro para +Z; `vehicle-physics.mjs` define nose/forward local -Z. `game.js` deve migrar o mesh inteiro para nose -Z e manter física, yaw, TrackPath, checkpoints, IA, rodas e câmera no mesmo frame; não mascarar com sinal ad-hoc.
- `game-input.mjs` oferece `poll()`+`dispose()`, sinais normalizados, deadzone 0.10, Gamepad standard/custom, disconnect/focus neutral; precisa integrar e testar no jogo.
- `race-ai.mjs` oferece política e `rankRacers`; precisa alimentar observações reais da mesma curva e atuar sobre os 15 carros com a física, não ser decoração.
- A imagem única e a cena atual impõem assets autorais/procedurais; sem licença ou disponibilidade de modelo F1, não declarar igualdade visual a produção comercial. Objetivo: elevar o máximo possível com geometria/material/texture detail e capturas reais.

ARQUITETURA, UNIDADES E CONTRATOS
- Manter a stack existente: HTML/CSS/JS vanilla + Three.js 0.180 via CDN; WebGL render, API Gamepad nativa, Web Audio nativa. Não instalar bibliotecas e não adicionar gerenciadores.
- Sistema único: X lateral, Y cima, Z profundidade; vetor de avanço local do carro/modelo = (0,0,-1), direita local = (+1,0,0), yaw com convenção já definida por `vehicle-physics.mjs`; vetor mundo forward = `(-sin(yaw), 0, -cos(yaw))`.
- Única curva fechada TrackPath em `game.js`; samples arc-length dão center/tangent/right/yaw/curvature/height. Esta curva fornece ribbons da pista, limites/collision, checkpoints, AI look-ahead, ranking, grid e SVG minimapa; `right=(-tangent.z,0,tangent.x)` quando tangent é -Z.
- Unidades do mundo = metros; physics states são m/s; HUD speed=`abs(speedMps)*3.6` km/h; physics avançada em steps 1/120, acumulador de frame capped; race state recebe o tempo efetivamente simulado; pause congela AI+física+timer.
- Interface módulo de input: `createInputController().poll()` entrega `{steer:-1..1,throttle:0..1,brake:0..1,connected,gamepadId,mapping,rawAxes,rawButtons,cameraToggle,pauseToggle,reset,diagnosticsToggle}`; física não consulta teclado/Gamepad diretamente.
- Interface física: `createVehicleState({x,y,z,yaw,speedMps,lateralMps})` + `stepVehicle(state,controls,dt,config)`; jogador e 15 AI usam o mesmo integrador.
- Interface AI: observar orientação/lateralidade em relação ao sample futuro, curva/velocidade-alvo e lanes ocupadas; `updateOpponentDriver(driver,observation)` retorna controles normalizados; colisão/manobra é progressiva, sem teleporte.
- Interface da corrida: `createRace(4,3,{initialTimeMs:58000,...})` → `beginCountdown` → loop `advanceRace(dt)` → somente cruzamento geométrico sequencial `passCheckpoint`; bônus real pelo checkpoint; ranking usa progresso mundial monotônico real e `rankRacers`; ESC/Options pausa e congela simulação.
- UI do screenshot liga todos os valores à sessão real; dificuldade escolhida altera AI; time countdown inicial 58s (extensão nos checkpoints); tempos por volta/volta anterior e 16 posições; minimapa desenhado do mesmo array TrackPath e posições reais de carros.
- Áudio via WebAudio criado/retomado apenas após click Iniciar: motor oscilar/ganho acompanham m/s; sons de countdown/checkpoint/collision/finish são sintetizados por nodes/oscillators, sem arquivos de som/dependência.

CRÍTICOS INDEPENDENTES JÁ EXECUTADOS (pré-implementação)
- C15 Visual (`ses_f2ea8b91fffe0AfuZm1TtgtQqt`): baseline visual não é F1; carro ciano ocupa ~9% x referência ~38%; environment e HUD divergentes; cena low-poly plana; trecho cego com estado A corrida vs B menu.
- C00/C04/C05/C06/C08/C09/C10/C14 auditoria técnica agregada (`ses_f2ea8b901ffelcs8gGl2Dkksk7`): física S/reverse falha em `vehicle-physics.mjs:64–68`; testes 54/55; corrida total 0 porque integração não chama avance; `game.js` não consome Gamepad; módulos coordenadas conflitam +Z/-Z; IA módulo não invocado; uma câmera sem cockpit/elevated; sem HUD classificação/timer/kmh/minimapa.
- Os relatórios foram independentes, somente leitura, citações de evidência acima; cada correção será submetida a nova avaliação separada e testes de regressão.

EXECUÇÃO MULTIAGENTE REAL E LIMITES
- Runtime do orquestrador exposto nesta conversa: `webajato/cx/gpt-6-luna`; nenhuma opção de esforço raciocínio exposta; não afirmar que Ultra/Max foi aplicado.
- Ferramenta de tarefas efetivamente habilitou dois críticos simultâneos (`explore`, sem edição) com IDs acima; nenhum controle de seleção de modelo/esforço aparece na API, nenhum nome de especialista/configuração fictício será inventado. Não mudar o modelo atual.
- Implementação da integração e apresentação: mySystem (orquestrador/editor desta worktree). Pacote de áudio será atribuído a um segundo agente se formato de execução puder alterar arquivos independentes; mudanças no mesmo `game.js` ficam sob mySystem para impedir conflitos.
- Desenvolvedor de cada módulo existente reportado no histórico anterior: `game-input.mjs` Gibbs; `race-ai.mjs` Darwin; `vehicle-physics.mjs`/`game-logic.mjs` mySystem. Autoria/histórico não transforma esses desenvolvedores em aprovadores.

PLANO DE TRABALHO (implementação autorizada)
[x] Ler New Prompt.md integralmente, referência anexada, memória e implementação; ignorar pastas `.git`, `.vs`, `.idea`.
[x] Examinar captura baseline efetiva antes das alterações desta missão e análise independente da referência.
[x] Executar toda bateria dos quatro módulos; registrar bloqueador físico de frenagem.
[x] Registrar arquitetura, dependências, pacotes/arquivos, riscos, critérios e avaliações prévias neste checklist ANTES de modificar código desta missão.
[ ] Corrigir physics braking: em passo com speed positiva e brake > 0, travar velocidade em 0; marcha à ré apenas num passo posterior mantendo brake; assegurar drag não introduz cruzamento inverso; adicionar regressão limite passo curto.
[ ] Integrar Gamepad + teclado/setas e comandos C/R/Escape/F2; normalizar em um só controlador; congelar input não focado/reconexão segura; HUD mostra status Gamepad e tela diagnóstico raw axes/buttons e normalized input.
[ ] Integrar corrida real: grid, countdown 3–2–1/GO, 58s, pausa/retoma, bônus, passagem física ordenada, tempos válidos, expiração, conclusão e restart; playtest determinístico/browser; corrigir cálculo totalTime/lap.
[ ] Migrar física de jogador/AI para physics -Z; nariz visual para -Z; verificar eixo matematicamente na reta e steering world ±X antes de expandir.
[ ] Criar 15 adversários 3D, controles AI contínuos conectados à TrackPath/física, curva-aware braking/lane change/obstáculos; colisão circular simples; ranking pela distância rodada e largada em grid 16.
[ ] Recriar modelagem procedural F1 vermelho/branco com rodas slick largas, nose/chassis/carbon tub/cockpit/halo, asa dianteira/traseira, suspensão/wishbones, detalhes; sem tratar asset como modelo licenciado.
[ ] Aprimorar circuito/ambiente gerados a partir de TrackPath: superfície com textura procedural real via canvas texture, rubber/grain/linhas, zebras segmentadas, guardrails, áreas escape/grass; colinas/montanhas em malhas, floresta procedural, nuvens, lago, stands e sinalização coerentes com screenshot.
[ ] Integrar 3 cameras (traseira primária screenshot, cockpit, elevada), smoothing/FOV at speed e teclas C/Gamepad sem alterar física.
[ ] Recriar HUD screenshot em overlay: rank/16 + strip; timer 58/extend; current/last lap; km/h; minimapa TrackPath/player/opponents; beginner/difficulty; menus início/pause/results e settings dificuldade/qualidade.
[ ] Implementar Web Audio após interação: som motor proporcional à velocidade e eventos; desligar/suspender em pausa, disposal/restart adequado; validação em Chromium.
[ ] Implementar níveis Low/Medium/High via sombras, pixel ratio/densidade; reutilizar geo/material, instancing para floresta/veículos se útil; FPS reportado somente após medição em resolution+device.
[ ] Executar testes unitários 4 módulos, adicionar/ajustar regressões de integração necessárias; browser desktop gameplay e mobile overflow; testar pause/restart/Gamepad mock; dispositivo físico pendente se não disponível.
[ ] Executar crítica visual e técnica independente posterior; corrigir defeitos prioritários; comparar nova captura A/B cega vs baseline sob estado/enquadramento equivalentes e comparação identificada contra `docs/modelo.png`.
[ ] Atualizar memória e relatório final objetivo; requisitos sem evidência continuam [ ]/bloqueados; não alegar AAA comercial.

PACOTES, PROPRIEDADE DE ARQUIVOS E AVALIADORES
- ARCH/COORD-01: integração TrackPath/frame em `game.js`; dev mySystem; crítico C04 física e C07 camera via sessão independente posterior; criteria: tangent/right/forward -Z alinhados e reteste vetorial.
- PHYS-01: `vehicle-physics.mjs`, `vehicle-physics.test.mjs`; dev mySystem para bug existente; crítica prévia C00/C04 (`ses_f2ea8b901ffelcs8gGl2Dkksk7`), nova revisão separada pós-fix; aceite freinagem atinge zero antes da ré, limites e invariância dt.
- INPUT-01: `game-input.mjs` existente + integração exclusivamente `game.js` + HUD HTML/CSS; controller original autoria histórica Gibbs; integrador mySystem; C06 evidência prévia + avaliação post-integrada por agente independente; aceite sinais, axes/triggers gradual, dpad, neutral disconnect, C/R/Esc, diagnostic. Hardware físico: PENDENTE até existir device.
- RACE-01: `game-logic.mjs`/test existentes, integração `game.js`; dev mySystem; C10 crítica prévia; nova C10 post; timer countdown, pause, full timing, checkpoint bonus, 3 laps/time expiry/results/restart.
- AI-RANK-01: `race-ai.mjs` e testes existentes + `game.js` integração; autoria histórica Darwin; integrador mySystem; C08/C14 crítica prévia e nova crítica pós; all 15 motors simulated, rank uses TrackPath real, no teleport, bots brake curve.
- VEHICLE-WORLD-01: geometry/environment/physics/collisions `game.js`; dev mySystem; C02/C03/C11/C13 reviewers independent sequentially; F1 red/white 3D/model/track physical coherence, FPS measured.
- HUD-CAM-01: `index.html`, `styles.css`, camera+HUD integration `game.js`; mySystem; C09/C07 + critic visual C15 separate; compare actual reference and paired gameplay screenshot.
- AUDIO-01: new file candidates `racing-audio.mjs`, `racing-audio.test.mjs`—indispensable to isolate WebAudio lifecycle and deterministic mapping, registered before creation; dev independent agent somente se tiver escrita disjunta; C12 separate read-only reviewer; tests engine freq monotonic / brake / resume/dispose. Fallback if agent can't implement: mySystem in `game.js`, no new file unless new justification recorded.
- PERF-QA/C14/C00: critics no write `game.js`; measure renderer.info/fps, gameplay and complete race; crit C00 independently observes integration. No fake reports/agent personas.
- Parallelization: 2 real task slots evidenced in this session. Reviewer domains can inspect disjoint artifacts concurrently; no two writers on same file. Subagent model+effort selection unavailable, so retain current configured session model without overrides.

ARQUIVOS E RISCOS
- Alterar existentes: `game.js`, `index.html`, `styles.css`, `vehicle-physics.mjs`, `vehicle-physics.test.mjs`, possivelmente `game-logic.mjs` e seu teste; `checklist.txt`, memória em `docs`.
- Criar previamente aprovados: `racing-audio.mjs`, `racing-audio.test.mjs` somente se pacote independente conseguir implementá-los; nenhum asset/gerador/dependência externa novo.
- Remover: NENHUM; executar mudança destrutiva de arquivo permanece proibido.
- CDN Three.js requer rede; sem pacotes/build tools no projeto. Gamepad real pode não estar plugado; testes API não provam teste de hardware. WebGL/fps variam por GPU. Assets procedural não atingem fotorealismo comercial; documentar qualidade e measured FPS. O ciclo de interação exige HTTP, not `file://`.
- Corrida completa de 3 voltas + 15 AI exige gameplay scriptável para cobrir sem controles físicos; controles devem também ser validados com W/A/S/D. Screenshot image 1672×941 e baseline 1440×900 precisam normalizar exibição apenas, preservando originais fora workspace.

CRITÉRIOS DE ACEITE (NÃO APROVAR SEM EVIDÊNCIA)
[ ] jogo inicializa sem exceção/HTTP failure.
[ ] 3D real; monoplano 3D procedural F1 vermelho/branco + camera third-person close; TrackPath fechado, barriers, collision.
[ ] W/↑ frente no vetor nariz local -Z; S/↓ brake e ré depois stop; A/← left, D/→ right; C cycles 3 cams; R reset; Escape pause.
[ ] Gamepad API dita eixos -left/+right, throttle/brake proportional, D-pad, C/options, disconnect neutral; diagnostic F2; hardware físico declarado pendiente quando ausente.
[ ] tempo real 58s, countdown, extensão, sequência de checkpoints, 3 voltas, ranking 1/16, pause frozen, timeout/end/restart.
[ ] 15 AI percorrem TrackPath e disputam em coordenadas; todos 16 aparecem no ranking/minimap; sem teleporte.
[ ] speed km/h corresponde `abs(speedMps)*3.6`; current/last lap; ranking/progress dynamic; map derives same sampled centerline.
[ ] ambient/music effects Web Audio unlock on interaction; motor freq tied to speed, pause/resume/dispose safe; no missing audio dependencies.
[ ] screenshots real render compare ref/baseline/final in same viewport; all browser exceptions/resources and controls reported.
[ ] full tests pass; main controls physics integrated; quality options Low/Med/High; FPS measured and actual info recorded.
[ ] per-module independent reviews real and complete; no approval self; user-specified model only if selectable; effort only if exposed.

HISTÓRICO DE CRÍTICA / CICLOS
- Pré-cycle 0: auditor visual confirmou defeitos listados; auditor técnico reproduziu friction crossing zero and missing integration; 54/55. Correções ainda não implementadas.
- Registro pós-fix obrigatório: versão/evidência/symptom/cause/change/test/independent retest.
- A/B cegas só com gameplay/câmera/viewport comparáveis; baseline menu vs ref live é incompleta e deve ser marcada, não convertida em nota sem contexto.

================================================================
NOVAS OBSERVAÇÕES DO PLAYTEST — 2026-09-24
- Usuário forneceu capturas do runtime e relatou: água invade/cobre a pista; pedra/montanha atravessa o traçado; rivais não se movem ou seguram o player; pista pequena e deve se aproximar de Interlagos; veículo atravessa montanha/piso quando sobe/desce; carros empilham; carro se autoacelera; fotos adicionais têm direção mais realista.
- Evidência reproduzível anterior no CDP: input W alcança >110 km/h, percorre pista e checkpoint 1; depois desacelera a 0,4 km/h ao persistir overlap com rival (~2,3m); diagnóstico F2 mostrou `carro 13 (2.3 m)`. Portanto colisão circular aproxima carros, aplica perda repetida e pode imobilizar o player.
- Código: `advanceSimulation` obriga `throttle >= 0.58`, causa autoaceleração mesmo sem W/trigger. Grid e pares precisam ser reorientados/expandidos; resolução atual não impede stack vertical de carros, usa threshold circular e não garante progresso das 15 AI.
- Água: plano `lake` 190×120 posicionado em world `(111,0.025,-53)` intercepta plano de pista; não clipado ao exterior, não considera perfil Y.
- Pedra/montanha: cones largos/altos em `addMountainsAndLake()` cruzam enquadramento/pista; cenário repetitivo low-poly. Reposicionar no fundo, compor Interlagos, afastar de toda bounding corridor e evitar grandes triângulos visíveis.
- Elevação: `trackSample` fornece Y; malha ribbon usa alturas, mas ground global está em Y=-3.2, árvores e assets têm posições/colisores não ancorados; simulação vehicle-physics.mjs preserva `state.y`, colisões usam plano XZ/point snap, AI/position podem atravessar superfície. Condução deve amostrar height/surface normal, acompanhar transições e não corrigir atravessando terreno.
- Interlagos: cálculo crítico do centerline atual ~239.7m pela spline, AABB X≈80/Z≈68 (trilho genérico); isso atualiza estimativa antiga de ~600m. Meta de redesign ~4.3km, ordem de setores Interlagos reconhecíveis; pós-smoothing precisa recalcular arc length e não ultrapassar a meta sem declarar.
- Referência vigente: usuário explicitamente selecionou a nova screenshot top-down navy/cyan para SUBSTITUIR F1 anterior como direção visual. O jogo continua corrida/interlagos; formato top-down governa paleta/UI, não altera mecânica em Tower Defense. Fotos realistas F1 antigas são referências de carro somente quando não conflitem; usuário pediu aproximação realista, assets gerados externamente não autorizados ainda.
- C02 independente (`ses_f2d3c3980ffe9hAC21kp1gLqYv`) calculou água AABB `(x=16..206,z=-113..7,y=.025)` cruzando centreline perto `(24,-6)` onde asphalt Y≈-1.4; água está ~1.4m acima e cobre trecho. Montanha atrás `i=8` center ≈(-28,23,-62), width/base radius~33m/height49m; centreline `(-27,1,-34)`, gera mesh que invade corredor aproximado ~2.4m; pico colisão ausente. Solo global y=-3.2 vs pista profile -1.4..2.3. Carro contém physics state Y invariável. GRID ±2.05m lateral separation 4.1m, front wing width4.25 → overlap visível ~0.15m; SAT width half-extent 1.3 → visual collision false negative.
- Carro preso confirmado na rodada de playtest anterior: diagnóstico F2 `carro 13 (2.3m)` no entorno e velocity derruba para ~0.4km/h por penalidade repetida; collision runner atual não mede Y bounds, não aplica impulse response. Screenshots dão indícios visuais de AI stack; crítico C15 não conseguiu reproduzir stack em build atual por servidor indisponível, registro disto explícito.

SKILL THREE.JS AUTORIZADA
- Usuário explicitamente substituiu pedido anterior “não usar Skills” e agora solicita usar suíte threejs-game-skills, primeiro diretor e depois especialistas pertinentes. Suite `skills/threejs-*` encontra-se dentro do projeto; não há necessidade comprovada de instalar dependência npm.
- [x] Ler `skills/threejs-game-director/SKILL.md` e obedecer roteamento para especialistas pertinentes.
- [x] Ler skills/referências selecionadas para gameplay, AAA graphics, UI, debug-profile, QA-release, model integration, image, audio; stack existente impede instalação npm, assim foi usada a bundle local solicitada sem instalar dependência.
- O tool `skill` não lista esses IDs nesta sessão; skills locais podem ser lidas diretamente nos caminhos do workspace e seguidas sem afirmar que foram instaladas externamente.
- Nenhum pacote de terceiro/dependência deve ser instalado sem autorização prévia; “instalar skills” interpretado como utilizar a suite presente no workspace. Confirmação não necessária enquanto recursos locais existem.

CORREÇÕES PRIORITÁRIAS DO LOOP FÍSICO/GRÁFICO
[ ] Aplicar topologia/elevação comprovada: base/terrain abaixo do mundo, ground sampled profile do circuito sem triângulos cruzados, water só exterior e altitude/bounds inferiores; nada de água sobre pavement.
[ ] Corrigir pose car: visual chassis nose -Z; wheel anchors/suspension road-normal e height; physics Y/vertical velocity ou surface projection calculados contínuos; veículos jamais entram na malha da pista ou terreno nas rampas.
[ ] Grid inicialmente sem sobreposição (modelo ~5m × 2.7m): spacing longitudinal >=8m e lateral >=3.4m; duas colunas/F1 grid; orientar pelo tangent; todos colocados antes do countdown sem sobreposição física inicial.
[ ] Colisão por footprints orientados no frame track-local com separação contínua; impedir interseção vertical e stack: pares 2D com overlaps resolvidos, cars deixam de reduzir velocidade persistentemente; colisão dá impulso lateral real e damping uma vez; AI reage/ultrapassa de modo contínuo.
[ ] Remover aceleração automática `Math.max(throttle,0.58)`; estado neutro com carro parado/freio no grid; não auto-acelerar no GO ou fora do W/arrow/trigger. Autodireção assistida não altera comandos do player nem acelerar por padrão.
[ ] Fazer física manter valor `y` e integrar ramp slope, sample chassis/wheels contra superfície/normal; termos de normal gravity/ground contact. Testar altitude antes/depois da lombada, velocidade de subida, descida, viaduto, zona de escape.
[ ] Revisar mapeamento curva ↔ tangent ↔ yaw ↔ sign: vector -Z; steer<0=left; trajectory/clothoid/curvature, Player `trackStartProgress` separado de totalDistance; AI por `trackProgress`, mantendo cumulative progress para ranking.
[ ] Interlagos scale: aumentar curva geométrica ~7× com pontos realistas dos setores; amortecer câmera e FOV para manter compo da referência; coordenadas e scene scenery ficam bounded com o track.
[ ] Revisar ground/water/terrain bounds via bounding boxes/corridor e camera screenshots; cones montanha não podem sobrepor percurso nem skyline frontal. Paisagem longe em `far-field`/silhouette ridge fora da pista; lago atrás do guardrail, abaixo das pistas/trilhas.
[ ] Atualizar screenshot real após cada ciclo, rodar Playwright/CDP (ou ferramenta real instalada), analisar performance de fps, rival count, física/cameras, pause/restart/AI lap; crítica independente posterior e regressões.

AGENTES/CRÍTICOS — NOVO CICLO
- Diretor: `skills/threejs-game-director/SKILL.md`; mySystem coordena arquivos comuns após ler o roteiro.
- Especialista gameplay/física/AI/circuit: implementação e revisão ao vivo; C02 circuito, C04 física, C05 teclado, C06 gamepad, C08 AI como críticos independentes.
- Especialista AAA graphics/environment/camera: implementação de materiais/escala/track reference; críticos C01 render, C03 car, C07 camera, C11 environment, C15 blind visual compare.
- QA/performance: crítico C14 e C13 após integração; C09 HUD; C10 game state; C00 integração geral.
- Nenhum modelo/esforço alterado; harness expõe GPT-6 Luna ao principal, mas effort sem controle. Child task `explore`/`general` sem model/effort fields; não declarar config dos child agents. Revisores seguem identidade por responsabilidade, independentemente, mas ferramenta pode não fornecer modelos de “crítica” reservados.
- Semagentes escritos concorrentes em `game.js`; reviewers não editam. Pacote audio separado e já implementado pode ser auditado sem alterar scene/game simultaneamente.

================================================================
PACOTE DE INTERFACE — HUD DA REFERÊNCIA — 2026-09-24
Escopo desta solicitação: implementar a interface correspondente às seções 16/17 de `docs/New Prompt.md`, usando `docs/modelo.png`; autorização explícita já recebida nesta conversa. Propriedade de implementação limitada a `index.html` e `styles.css`; `game.js` e módulos/testes não serão alterados.

PLANO
[x] Ler a memória de `./docs` em ordem cronológica, o checklist existente, `docs/New Prompt.md` (seções 16/17), `docs/modelo.png`, `index.html`, `styles.css` e os pontos de integração existentes em `game.js`.
[x] Confirmar o contrato integral de IDs solicitado, incluindo os valores exatos das opções de dificuldade e qualidade.
[x] Registrar o plano antes da implementação.
[x] Substituir a composição antiga por HUD fullscreen angular, sobreposição inicial compacta, estados para countdown/pausa/resultados/diagnósticos e minimapa SVG aproximado da TrackPath atual.
[x] Manter valores sem fonte real indisponíveis; seletores refletem escolhas, sem alegar integração com a simulação.
[x] Adicionar responsividade em 390px, foco de teclado, contraste, controles acessíveis e movimento reduzido.
[x] Validar IDs/opções, startup e overlays em Chromium; não editar `game.js`, módulos ou testes.
[x] Atualizar este relatório e a memória operacional com fatos e verificações finais.

ARQUIVOS A ALTERAR
- `index.html` — substituir o conteúdo da interface e incluir o contrato DOM pedido, seletores e markup acessível do HUD/overlays.
- `styles.css` — substituir a apresentação time trial por HUD fullscreen responsivo, angular, escuro translúcido e alinhado à referência.
- `checklist.txt` — registrar plano, execução e relatório desta solicitação.
- `docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md` — acrescentar resumo factual e validações, sem cadeia de pensamento privada.

ARQUIVOS A CRIAR
- NENHUM.

ARQUIVOS FORA DO PACOTE / PRESERVADOS
- `game.js`, `game-logic.mjs`, `game-logic.test.mjs`, `game-input.mjs` e seus testes, `race-ai.mjs` e seus testes, `vehicle-physics.mjs` e seus testes: não alterar.
- Remover: NENHUM.

RISCOS / LIMITES
- A implementação atual em `game.js` ainda é time trial e não fornece ranking de 16 carros, minimapa dinâmico, countdown/extend-time, pausa, Gamepad integrado, velocidade no DOM, perfil gráfico ou dados de câmera alternável. A interface não inventará esses estados; os pontos sem fonte real permanecerão vazios/ocultos.
- Manter compatibilidade dos IDs legados ainda consumidos por `game.js`: `race-app`, `game-container`, `race-status`, `timer-value`, `lap-value`, `start-button`, `results-panel`, `result-time`, `result-best` e `restart-button`.
- CSS mantém canvas interativo, seletor/foco visíveis e leitura utilizável em viewport de 390px.
- Modelo efetivamente exposto nesta sessão: `webajato/cx/gpt-6-luna`; nível de esforço não é selecionável/exposto nas ferramentas desta execução, portanto não será alegado.

EXECUÇÃO
[x] Plano registrado antes da implementação.
[x] `index.html` e `styles.css` implementados; nenhum arquivo de jogo/módulo/teste alterado.
[x] 35 IDs únicos presentes; opções dificuldade `easy/medium/hard`, gráficos `low/medium/high`; Chromium HTTP confirmou viewport/documento 390×844, painel dentro dos limites, início da corrida, F2 e reduced-motion sem exceções.
[x] Bateria existente: `node --test game-logic.test.mjs game-input.test.mjs race-ai.test.mjs vehicle-physics.test.mjs` — 55/55 aprovados.
[x] Capturas reais Chromium: desktop 1440×900 e mobile do modo headless; HTTP/CDP validou geometria exata em 390×844. Nenhum repositório Git presente; `git diff` indisponível, revisão limitada aos arquivos e ao escopo de patches executados.
[x] Memória e relatório final atualizados.

RELATÓRIO FINAL
Arquivos criados: NENHUM.
Arquivos alterados:
- `index.html`
- `styles.css`
- `checklist.txt`
- `docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md`
Arquivos removidos: NENHUM.
Resumo por arquivo:
- `index.html` — HUD e sobreposições acessíveis, opções exatas, mapa SVG aproximado, diagnóstico F2 oculto até abrir e status Gamepad com dados disponíveis; valores sem fonte permanecem indisponíveis.
- `styles.css` — HUD fullscreen angular translúcido, composição alinhada à imagem e ajustes responsivos/acessíveis.
- `checklist.txt` — execução, verificação e limitações documentadas.
- Memória — resumo factual desta implementação e evidências.
Como validar: servir via HTTP, abrir o jogo e conferir HUD, seletor, início, F2 e viewport 390×844; executar `node --test game-logic.test.mjs game-input.test.mjs race-ai.test.mjs vehicle-physics.test.mjs`.
Limites: contagem regressiva, pausa/retomada, classificação, velocidade, minimapa de participantes, seleção de gráficos, câmera e resultados da corrida ainda não são alimentados pela integração de `game.js`; nenhum valor foi simulado. `git diff` indisponível pois a pasta não é repositório Git.
Modelo reportável: `webajato/cx/gpt-6-luna`. Esforço máximo: indisponível/não exposto pelo harness.

================================================================
CONTINUIDADE THREE.JS — UPDATE PLAYTEST USUÁRIO — 2026-09-24

SUBSTITUIÇÃO VISUAL APROVADA
- Usuário escolheu substituir F1 (`docs/modelo.png` e fotos F1) como referência visual pela nova screenshot top-down navy/cyan anexada.
- Gameplay continua corrida de carros: screenshot orienta direção visual/HUD e paleta, não converte o objetivo para tower defense nem autoriza inventar wave/threat/core/credits.
- Nova direção: navy/preto, borders finos teal/cyan, painéis concisos/angulares e mapa esquemático claro; HUD continua com ranking/timer/checkpoints/laps/speed/track/difficulty/pause/results da corrida real.
- Brief vigente: corrida arcade em Interlagos; primary verb conduzir W/S/A/D, setas/gamepad; desafio = curvas/tráfego/checkpoints sob tempo; checkpoint recompensa +15s; conclusão por voltas/expiração e feedback de resultado + restart curto.
- Interlagos será aproximação procedural com setores reconhecíveis, não survey; alvo de percurso ~4km e uma TrackPath comum a visual, física, AI, ranking, minimapa e gates.
- Captura top-down não contém carro/câmera de corrida; veículos e terceira pessoa continuam exigidos pelo prompt e fotos realistas anteriores. Manter isso explícito na futura comparação.

SKILLS THREE.JS — REGISTRO DE USO
- Carregado diretamente do workspace `skills/threejs-*` (nenhuma instalação npm): director `skills/threejs-game-director/SKILL.md`; gameplay `skills/threejs-gameplay-systems/SKILL.md` + `references/physics-engine-selection.md`, `genre-design.md`; graphics `skills/threejs-aaa-graphics-builder/SKILL.md` + `references/visual-scorecard.md`, `authoring-recipes.md`, `technical-art.md`, `shader-cookbook.md`; UI `skills/threejs-game-ui-designer/SKILL.md` + `references/ui-patterns.md`; debug `skills/threejs-debug-profiler/SKILL.md` + `references/debug-playbook.md`; QA `skills/threejs-qa-release/SKILL.md` + `references/playtest-bot.md`, `visual-test-harness.md`, `release-checks.md`; 3D/image/audio integration skills + references.
- Física: manter custom arcade em timestep fixo; skill recomenda integration contracts, collider proxy simples e update order input→physics→collisions/state→UI/camera/render; não instalar Rapier porque contrato single-file vanilla e custom collision bastam quando estáveis.
- Graphics scorecard: nenhum claim premium até active gameplay, render diagnostics, no auto-fail, referências visuais/estado controlados; as screenshots atuais de defeito são evidência de reprovação, não score.
- Probes pagos: screenshot/procedural authorized sem model/assets externos do pacote; nenhuma credencial foi presumida nem chamada paga submetida.
- Playtest: preparar `__THREE_GAME_DIAGNOSTICS__` e `__THREE_GAME_TEST_HOOKS__` reais no game.js para estados starter/grid/active/pause/results e freeze capture; bot mede frames, distância, objective, softlocks e exceções; state request deve ACK após mutação real, nunca no-op.
- Interface deve ser reestilizada conforme nova captura após sistemas reais, mantendo leitura da pista, UI não cobre herói/ameaça, e liga apenas state único real.
- Nenhum skill exigiu novo pacote/npm; nenhum arquivo criado apenas por leitura. `artifacts/game-progress.md` poderá ser adicionado se indispensável a continuidade director e listado/justificado antes de criar.

NOVOS DEFEITOS BLOCKING — SCREENSHOTS/RELATO DO USUÁRIO
- Água/plano lake passa visualmente por cima da pista: remover do footprint e das vistas da curva; bound/layer/altura corretamente abaixo/fora do track.
- Rochas/montanhas gigantes intersectam câmera e pista: aproximam-se de cone procedural grande; retirar formas visíveis do corridor de gameplay, usar relevo distante lateral/de fundo.
- Rivals não progridem/seguram carro e screenshots exibem carros empilhados: grid e collision strategy não suportam 16 footprints. Separar locais de spawn (longitudinal + lateral) e substituir circles por oriented footprint/track-local collision, resolver depenetration com velocidade/impulso uma vez, observar safe gap/AI lane avoidance; testar estado parado/player approach. Nenhum carro pode ficar fisicamente sobre outro.
- Autoaceleração explicitamente vem de `Math.max(throttle,0.58)` no `advanceSimulation` (W não mantida, mas a engine ainda aplicava input artificial); remover. Neutro deve manter veículo parado/coast natural sem aceleração aplicada; countdown não aplicar freio/aceleração escondidos; somente teclado/joystick controla throttle.
- Estabilidade do código/resultado é duvidosa: `trackProgress` etc em worktree mudaram recentemente; rodar ESM parse + CDP depois de cada etapa para evitar regressões ocultas.
- Terreno e pista vertical divergem: fórmulas de física preservam y horizontal e guard rails não acompanham slope; malha/vehicle vertical precisam sample contact heights, suavizar `state.y`, cancelar colisão fora do plano XZ/ramp. Validar ascend/descend por y delta e rodas no pavement.
- Escala/tipo de pista: usar layout de Interlagos com nome/ordem dos trechos e dimensão suficiente; manter cones/mountains/water/trees “outside track corridor”.

PLANO NEXT LOOP (continua autorizado)
[ ] Corrigir de imediato input neutral throttle; testar nenhum comando → m/s nunca aumenta artificialmente; held W aumenta positivo, release coast, S freia e ré só depois parar.
[ ] Implementar e testar grid 16 lugares sem sobreposição em interseções considerando model bounds reais com orientation -Z; clearance min longitudinal ≥ model length+2m e lateral ≥ width+1m.
[ ] Collision design: bounding oriented boxes no XZ + vertical interval/track-local longitudinal/lateral; one-frame contact edge + restitution/damping/depenetration; clearance test artificial para 15 cars, no y integration stacking, overlap recovery sem zero-speed lock; preserve world height offset.
[ ] Corrigir IA: progress progress physical sample + lapDistance; lane classification relative to track tangent; avoid occupied lanes and threshold safe gap; drive controls actual; no blocker de fila enraizada na line. Telemetry record 15 rivals positions before/after 30s; AI laps/progress; no teleport.
[ ] Track real Interlagos: tracing local, 2D centerline with turns labeled corresponding order. Extend to ~4km in game units; use survey/public facts only if available, else label “aproximação procedural”. Prefer path with unique sectors over scaling existing simple 600m loop by factor 7 (wide overshoot risks), calculate m length. Single TrackPath generator drives all derived data.
[ ] Track width from ~15m current to ~14-18m stable at scaled geometry (camera/model sizes compatible); elevation bands 0-30m plausible consistent. Create road/runoff with triangles connecting each profile sample in xyz; ground terrain conform elevations/lift and fit exactly, no global plane under hills. Water only on outside lake bath below nearest shoreline, its mesh bounds disjoint road/border corridor. Use heightfield/trackside valley segmented.
[ ] Terrain rocks/mountains: place within bounded outer ring, compute bounding spheres and minimum point-to-track >= track-half-width + radius + safety; hide/cull all in road corridor. Camera validates before producing captures.
[ ] Huds re-theme navy/cyan per latest chosen screenshot; retain actual values. Rank 01/16 etc; no invented game modes. Keep menu/overlays fitted.
[ ] C12 audio tests remain 5/5; connect no collisions to emit only real contact edge; fix duplicate result event order (pause before finish may silence finish cue); test dispose/restart/input focus.
[ ] Implement real diagnostics/test hooks + deterministic seed or account for no random; use browser CDP bot to hold neutral, race start, W, steer, release, follow multiple positions, timer/pause/restart; assert x/z/y movement and progress. Provide capture active gameplay and viewport 390 × 844.
[ ] C02/C04/C08/C11/C13/C15/C00 critical review after corrections via session independent read-only; use newly observed attachments and numeric state from F2. Fix concretely identified regressions and repeat tests.

MODELO/SESSÕES
- Sessão modelo pelo harness exposto `webajato/cx/gpt-6-luna`; nenhum controle de reasoning effort.
- Skills Three.js não foram instaladas em `~/.agents`; suite presente no checkout e lida integralmente segundo routing/referências pertinentes; sem pacote npm/dependência.
- Nova imagem top-down navy/cyan foi aprovada para substituir referência de art direction; screenshot F1 não governa mais paleta/HUD.
- Direção de HUD: navy/cyan, track map dominante, estatísticas reais de corrida agrupadas/concisas. Não implementar tower-defense wave/core/credits.
- Gameplay brief atual: fantasy pilotar Interlagos; feeling arcade de linha/cotovelo e tráfego; verb acelerador/freio/esterço com teclado+gamepad; reward checkpoint real +15s; progresso em 0–3 voltas; failure timer 58s ou meta concluída; restart rápido.
- Interlagos aproximado: reta principal/pits→S do Senna→Curva do Sol→Reta Oposta→Descida/Subida do Lago→Ferradura→Laranjinha→Pinheirinho→Bico de Pato→Mergulho/Junção→reta; uma curva geométrica de ~4.3km calculada/inspecionada, não levantamento survey.
- Evidence before next edits: estado runtime repro real (car reaches 112km/h, checkpoint 1, later blocked repeatedly by rival id 13 2.3m; no neutral test yet; forced throttle present) e capturas do usuário anexadas. Screenshots não podem provar problema ausente; CDP console/perf logs para versões futuras.
- Revisão crítica C04/C05/C06/C08/C14 efetivamente executada read-only (`ses_f2d9d16a3ffeWCZrMuSs5C54Ro`): confirma `vehicle-physics` signs -Z/steer +right coherentes; `stepVehicle` subdivide em ≤1/120 mas último substep pode ser parcial; AI fica em throttle=brake=0 se todas as faixas ocupadas e depende de caller timing 1/60; grid/stacking/ResetY ficam em `game.js`, fora de módulos auditados. Correção deve focar integração sem inverter signs da física.
- Lendo skill director `SKILL.md`: para build substancial precisa `artifacts/game-progress.md` como progresso/checkpoints atualizados. Criar este arquivo é necessário e previamente listado/justificado aqui; arquivo não será exclusivo fonte de auditoria (checklist permanece central).
- Proposta de escritor do pacote HUD: task `ses_f2e8cf331ffeGwZ6PWOn3V9cXr` atualizou `index.html`/`styles.css`; tarefa documentada no checklist anterior. Implementação F1 mantém pendências de integração e o art direction do HUD já é substituído pela screenshot nova.
- Em critério QA skill hooks: `window.__THREE_GAME_DIAGNOSTICS__` expõe renderer/state/FPS/position/progress; `window.__THREE_GAME_TEST_HOOKS__` com `seed` (sem rand, registrar determinístico), `setState(name)` com ack/state realmente aplicado e paused screenshot. Adicionar no `game.js` (arquivo permitido) antes de solicitar ferramentas.
- Operação workflow: o runner não instala skills fora de checkout; carregar diretamente por read foi utilizado conforme nova permissão explícita. Nenhum modelo/esforço trocado/configurado.

PRÓXIMAS FASES ATUALIZADAS (reordenação por novos defeitos críticos)
[ ] Criar `artifacts/game-progress.md`: intenção/style top-down navy/cyan + gameplay Interlagos, constraints/status/bugs/done/pending/evidence/next IDs; arquivo criado só após sua autorização registrada acima.
[ ] Desligar qualquer auto-throttle: alterar `throttle: Math.max(throttle,0.58)` para throttle exclusivamente de input; nenhum controle resulta aceleração zero e velocidade inicial neutra.
[ ] Grid 16: 2–3 colunas com offset de heading/tangent e lanes width >=3.4m, row spacing >=7m; calcular rotated OBB car footprint 5.8×2.8; assert min car pair overlap <=0 no spawn.
[ ] OBB SAT XZ + vertical guard: project both vehicles onto local forward/right axes; só resolve quando footprints overlap AND height interval overlap; longitudinal/lateral separation position/impulse; add edge cooldown once, não `×.75` a cada substep persistent. Player/rivals em frame world anchored; nenhum estado Y por impulso de lateral; never vertical stack.
[ ] AI anti-stall: blocked lanes por distância circular/signed forward atual ≤8m (considerando lane local); mudar target lane, acelerar/continuar se à frente livre; vehicle at rest + blocker gone reaplica throttle em ≤250ms; atualizar lane offsets smooth/no collision overlap; rivals velocity/progress e lap counters avançam sem teleport.
[ ] Redesenhar centerline seguindo Interlagos e ajustar units para ~4300m após arcLength adaptation; checkpoint placements derivados de sectores/ordem; direção de cada curva não cruza em route mapping.
[ ] Aumentar `TRACK_HALF_WIDTH`/road carriageway e ângulo câmera conforme track meters/monoposto, manter F1 silhouette; processar via mesmo centerline samples.
[ ] Elevação: derive spline y profile por sector; sample height linear/interpolado com normal; track ribbons + shoulders + kerbs + rails base + collider track + wheel mesh position/state.y passam mesma altura; vehicle-physics states need y integration/contact height callback or game layer follows exact queried sample + vertical contact/ramp slope; underpass prevented by actual wheels/track height comparison.
[ ] Recolocar lago como volume/polygon bounded à direita e EXTERIOR da pista, abaixo deck level; shore buffer `minDistance(lake mesh, track centerline) >= halfWidth+20m`; jamais cobrir gray asphalt. Remover cones/mountain obstruction from playable corridor; backdrop restricted far boundary and camera/horizon, not coarse triangles across road.
[ ] Reestilizar `index.html`,`styles.css` conforme navy/cyan tactical screenshot (HUD only race metrics), manter overlay/racer healthy. Run UI fit desktop/mobile.
[ ] Playtest F2 neutral→GO, nenhum input idle, W/↑ accel, S braking then reverse, A/D signs, Gamepad W/trigger via API and real device pending if absent; 15/16 vehicles, hill up/down, wide race, collisions, AI lane opening, 3 laps, +15s, pause freeze, finish/timeout/restart.
[ ] Existing screenshot testing re-use CDP profile; add actual page diagnostics/test hooks for bot; capture F1 gameplay UI in chosen cyan direction; if performance/gpu software measure renderer/fps only as actual.
[ ] Critiquer independent (session tasks) run live screenshot/gamelog while lead owns modifications, then rerun 60+ module tests/integration after code changes; record separate judge evidence.
- Files next: `game.js`, `vehicle-physics.mjs`, `vehicle-physics.test.mjs`, potentially `game-logic.mjs` and tests, `race-ai.test.mjs`, `index.html`, `styles.css`, checklist/memória; new `artifacts/game-progress.md` (previously justified).
- Files preserved/no deletion: none removed. New tests added only if needed to catch collision/grid/neutral throttle physical regression; add after criterion in checklist (covered by test file already listed). No package install.
Revisão final após F2 e ajustes de layout: 35 IDs únicos; opções verificadas; startup via HTTP passa; tela inicial sem diagnóstico exposto; F2 abre/fecha; foco visível no seletor; corrida inicia e oculta CTA; viewport/documento 390×844 sem overflow; reduced motion ativo; zero exceções; 55/55 testes.

EXECUÇÃO CONCORRENTE AUTORIZADA — USUÁRIO 2026-09-24
- Max slots solicitados: todas tarefas independentes permitidas pelo harness. Execução observada será medida pelo número de `task` calls ativas; perfis GPT/effort individuais não são controláveis neste harness e não serão inventados/trocados.
- Escritor SLOT 1 `index.html`, `styles.css`: HUD navy/cyan top-down escolhido, direção aprovada; conservar DOM IDs dinâmicos, nenhum estado tower defense. Crítico posterior C09/C15.
- Escritor SLOT 2 `race-ai.mjs`, `race-ai.test.mjs`: corrigir stall/deadlock e overloads usando target lane/reaction/response sequencing; integrar só sobre contrato de AI atual, não editar `game.js`. Críticos posteriores C08/C14.
- Escritor SLOT 3 `vehicle-physics.mjs`, `vehicle-physics.test.mjs`: planejar API de surfaceY/normals/vertical contact mantendo physics inputs/options existentes; preservação backward tests fixed-dt/braking/steer. Crítico posterior C04.
- Revisor SLOT 4 C00/C04 collision physics `game.js` readonly: início sessão tarefa futuro; encontrar cálculos e defender critério anti-stack/fixedheight.
- Revisor SLOT 5 C15 Visual `game.js`/captures somente leitura: comparar navy/cyan nova screenshot, testar collision/scene water/Interlagos honestamente.
- Integrador principal `mySystem`: exclusividade para `game.js`, edge-contact/grid/OBB/callback height, arquivos checklist/memória/game-progress; não tocar nos paths dos escritores concurrentes antes entregarem, integrar em seguida; validação unificada no Chromium depois de patch group.
- Idas requeridas: cada pacote reporta identificação/arquivos/testes; o executor do pacote não aprova; reviewers só aprovam após ler diff/captura/diagnostics executados. Reviewer bloqueia com bug reproduzível.

================================================================
PACOTE HUD-DEV — RETOMADA VISUAL — 2026-09-24
Status: planejamento concluído; gatilho explícito já recebido (“faça sua mágica” + `/goal`) e execução multiagente autorizada.

PLANO
[x] Ler `docs/New Prompt.md`, seção 16, e o brief aprovado em `checklist.txt` sobre a captura navy/cyan top-down.
[x] Ler `skills/threejs-game-ui-designer/references/ui-patterns.md`, `index.html`, `styles.css` e os seletores/IDs consumidos por `game.js`.
[x] Consolidar a camada CSS ativa navy/cyan na folha-base; eliminar a duplicação em camadas/overrides existentes, sem anexar uma terceira camada.
[x] Ajustar somente estrutura/labels/roles necessários em `index.html`; preservar IDs dinâmicos existentes e os estados do jogo.
[x] Aplicar os critérios: navy/cyan, HUD legível/responsivo, pista limpa, minimapa integrado a TrackPath.
[x] Contrato de IDs estáticos e Chromium HTTP desktop/mobile validados por mySystem; capturas finais desta retomada registradas fora do workspace.
[ ] Crítica independente C09/C15 pendente; não atribuir aprovação inexistente.

DECISÕES E LIMITES
- Direção visual aprovada: screenshot top-down navy/cyan; adaptar chrome/painéis ao HUD da corrida. A captura não muda gameplay nem câmera para tower defense.
- Gameplay permanece corrida arcade em Interlagos; não criar waves, torres, inimigos de tower defense ou métricas sem contrato.
- Preservar IDs usados por `game.js`: `#race-app`, `#game-container`, `#race-status`, `#timer-value`, `#lap-value`, `#extend-banner`, `#lap-time`, `#last-lap`, `#speed-value`, `#minimap*`, `#difficulty-value`, `#controller-status`, `#diagnostics-panel`, `#diagnostics-value`, início/pausa/resultados e demais seletores confirmados na leitura.
- Não alterar `game.js` nem módulos/testes. Não criar funções, scripts ou ações HTML; manter ações sob responsabilidade do JavaScript existente.
- Não alterar água, montanhas ou qualquer visual de mundo 3D neste pacote.
- Minimapa deve permanecer ligado à geometria TrackPath atual; sem inventar outro mapa/dados.
- A referência visual aprovada está documentada no checklist e na memória; imagem top-down não está presente como arquivo local em `docs`.
- Revisão crítica HUD/UX (C09/C15) continua pendente para nova screenshot/branch vigente; nenhum revisor pode aprovar alterações que ainda não analisou.

ARQUIVOS A ALTERAR (após gatilho)
- `index.html` — somente markup/labels/roles mínimos compatíveis com o HUD e contrato DOM existente.
- `styles.css` — consolidar HUD navy/cyan na camada-base; responsividade, tipografia legível e composição que preserve a área principal da corrida.

ARQUIVOS A CRIAR
- NENHUM.

RISCOS / IMPACTOS
- `game.js` consulta os IDs de corrida diretamente; removê-los ou alterar contratos interrompe inicialização/atualização do HUD.
- Sobreposição excessiva pode ocultar pista/veículos; validar áreas de gameplay e resoluções estreitas.
- A pasta de trabalho não é repositório Git, portanto a branch indicada pelo usuário não pode ser confirmada neste ambiente.
- Este pacote só pode aprovar markup/UI; câmera top-down do mundo continua propriedade de `game.js` e não deve ser alterada pelo responsável do CSS.

PERGUNTAS PENDENTES
- Nenhuma. O usuário confirmou que “faça sua mágica” e `/goal` já autorizam a execução.

AUDITORIA AI/FÍSICA PRÉ-IMPLEMENTAÇÃO — 2026-09-24
- Crítico C08/AI `ses_f2d0dcb10ffeu5s2PfN1ycSnVd` somente leitura + runner: `race-ai.mjs` 15/15 testes, mas deadlock reproduzível quando todas lanes ficam bloqueadas; state speed ~0.75m/s em 1 s e 0.053m/s após 10 s. `game.js` envia blocked lanes por distância circular sem dizer à frente/trás; offsets iniciais ±2.4/5.2≈0.46 são arredondados ao targetLane 0; AI error lateral positivo empurra o veículo ainda mais para fora (x=2.4→5.241 em 1 s) e laneOffset armazenado não guia carro fisicamente. Policy chamada 1/120Hz versus smoothing/taxas documentadas em 1/60.
- Correções pedidas C08: target lanes start coerentes com grid; bloqueio só por carro à frente com gap seguro; steer corrige erro lateral; deadlock provoca follow/pacing e recovery ao liberar; smoothed input escalado por `dt` e validação física de trajeto.
- Crítico C04 `ses_f2d0dcabfffeVTAKArqZdHafnA`: `vehicle-physics.mjs` 8/8; `surfaceHeight` não é lido e `y` se preserva exatamente. Reprodução y=2 + callback surfaceHeight=12 -> y=2. Próximo pacote PHYS-DEV deve atender e adicionar teste; integração `game.js` precisa prover callback da TrackPath.
- Fixedstep module divide em ≤1/120 mas subpasso final pode parcial; braking 1 chamada 0.5s difere 30×1/60. O jogo atual usa 1/120, então foco regressão API arbitrária + caller accumulation.
- C08 recomenda contato do AI/observation por gap positivo signed, não proximidade circular; contorno/adversary fairness medidos.

EXECUÇÃO — HUD-DEV
[x] Autorização já existente respeitada; implementação limitada a `index.html` e `styles.css`.
[x] `index.html`: viewport com `viewport-fit=cover`, labels compactos para tempos de volta e hint de controles; IDs preservados, sem scripts/handlers inline ou lógica de simulação.
[x] `styles.css`: CSS consolidado da base + overrides numa folha, de 1.982 para 1.353 linhas; portrait 320/390 e landscape 844, safe areas, foco visível, reduced motion.
[x] Chrome headless/CDP via HTTP abriu Three.js real, `data-state=ready`, canvas e botão ativo nos três viewports.
[x] Contrato estático: 44 IDs únicos; 37 referências `#id` diretas de `game.js` resolvidas; zero ID ausente, scripts inline ou handlers inline.
[x] Viewports `320×568`, `390×844`, `844×390`: documento sem overflow; menu, controles e CTA não colidem com painéis, velocímetro, mapa ou ajustes; menor texto secundário medido ≥12 px.
[x] F2 abre diagnóstico; valores dos seletores mantidos; `prefers-reduced-motion` verificado na folha.
[x] `node --test game-logic.test.mjs game-input.test.mjs race-ai.test.mjs vehicle-physics.test.mjs` — 62/62 aprovados.
[x] `node --check game.js game-logic.mjs game-input.mjs race-ai.mjs vehicle-physics.mjs racing-audio.mjs` — sintaxe aprovada.
[x] Servidor e Chrome de QA encerrados. Capturas reais fora do workspace: `%TEMP%\opencode\hud-final-portrait320.png`, `hud-final-portrait390.png`, `hud-final-landscape844.png`.
[ ] C09/C15: análise crítica independente da captura final pendente; sem claim de AAA/aprovação.

RELATÓRIO FINAL — HUD-DEV
Arquivos criados: NENHUM.
Arquivos alterados:
- `index.html`
- `styles.css`
- `checklist.txt` (rastreio obrigatório)
- `docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md` (memória obrigatória)
Arquivos removidos: NENHUM.
Resumo por arquivo:
- `index.html` — safe area de viewport, abreviações dos rótulos de tempo, descrição responsiva do hint; IDs e contratos intactos.
- `styles.css` — tema navy/cyan único, minimapa dominante, agrupamento do HUD por zona; mobile/landscape, safe areas, foco e reduced motion; overlay inicial reposicionado para desocultar o mundo e os painéis.
- `checklist.txt` — execução, evidências, limites e relatório final atualizados.
- Memória — histórico desta retomada e validações reais registrado.
Como validar:
- Executar `node --test game-logic.test.mjs game-input.test.mjs race-ai.test.mjs vehicle-physics.test.mjs`.
- Servir o projeto por HTTP e conferir `hud-final-portrait320.png`, `hud-final-portrait390.png` e `hud-final-landscape844.png` em `%TEMP%\opencode`.
- Conferir no navegador o botão Iniciar, estado pronto, F2, foco e preferência reduced-motion.
Harness reporta `webajato/cx/gpt-6-luna`; nível de esforço não selecionável/exposto. Nenhum agente especialista/revisor independente executou esta retomada; C09/C15 segue pendente. Sem declarar AAA.

AUDITORIA VISUAL C15 — nova direção aprovada, estática (2026-09-24)
- Task `ses_f2d3c394effeoQiyqxnJNNG6C5`; sem edição; CDP/HTTP indisponíveis quando rodou, portanto imagens antigas salvas fora de workspace não provam código atual.
- Findings: mundo ainda tem câmera third-person apesar de direção top-down target; Interlagos spline ainda precisa largura/tamanho/landmarks e escala (~80×68 antiga); lago e terreno não estão visualmente geometricamente conectados; mobile HUD 6px tinha sido acusado antes, corrigido pelo pacote HUD (menor secundário ≥12px segundo viewport test); F1 antiga supersedida não vale target de cor/HUD.
- Critic recomenda screenshots state ready e racing desktop/mobile sob mesma revisão e comparação identificada com screenshot top-down navy/cyan original (não arquivo local) e comparação world com fotos F1 anteriores apenas quanto a hero car.
- Áudio HUD UI: camada de HTML/CSS deve manter sem scripts inline no pacote NAVY-DEV. (Em revisão). Nenhum C15 pass até screenshots runtime atuais.

CHECKPOINT DE IMPLEMENTAÇÃO MULTIAGENTE — 2026-09-24
- Model checker do harness ativo: gpt-6-luna só na sessão principal; esforço não configurável; nenhum toggle ou seleção de modelo foi usado.
- PACOTES LANÇADOS: AI-DEV slot `race-ai.*` — recebeu novo gatilho/conversa autenticada e retorna status/testes; PHYS-DEV slot `vehicle-physics.*` — callback vertical+regressions; HUD-DEV slot `index.html/styles.css` — atualização navy/cyan UI; C04, C15 críticos somente leitura. Todos têm write sets independentes; integração/correção collision `game.js` fica com mySystem.
- pending: integrar callback surfaceHeight, corriger collisions/game-state/testhooks, rede Interlagos, assets scene, full bot.
- Imagens test atuais ainda visivelmente errôneas (água/superfícies). Não apresentar entrega final ainda.

CICLO 1 (implementação de mySystem + agentes, sessão em curso)
- `race-ai.mjs/test` dev slot AI-DEV executado: 18 testes aprovados; corrige lane offset fracionário, sinal lateral, dt 60/120, deadlock all-blocked recuperável, trafficBehind vs trafficAhead. Crítico independente encontrado no mesmo turno observa API/código atual. Ainda falta caller enviar trafficAhead/deltaSeconds e adaptar laneOffset a grid/road geometry.
- `vehicle-physics.mjs/test` dev PHYS-DEV: surfaceHeight callback interpolado a cada substep e 5 testes de ramp/contact adicionados; `node --test vehicle-physics.test.mjs` 12/12. mySystem já passa `surfaceHeight(x,z)` via nearestTrackSample no updateRacer; normal/chassis inclination/each-wheel suspension ainda não modelados.
- `index.html/styles.css` HUD dev: layout responsivo navy/cyan escolhido no screenshot top-down; IDs existentes preservados; 44 IDs únicas, 37 references resolvidas, 320/390/844 sem overflow, tamanho secundário >=12px; foco e reduced motion testados; crítica C09/C15 ainda não aprovou screenshot nova.
- `game.js`: largura 18m, curva setorial pelo perfil Interlagos (~4.955m avaliada pelo renderer), 9 setores checkpoints, fileiras de grid a 9m, 2 colunas lateral ±2.15m, ground ribbon em perfil Y e lake no backdrop abaixo y da pista/lateral 72m; novo far field via bounds fora do track; água retângulo antigo removida; barreiras InstancedMesh.
- `game.js`: throttle floor removido; C08 confirmou inexistente no estado atual. Browser bot F2: nenhum input=0 throttle/speed; W real atinge 49.7 m/s e percorre 138m. Countdown→GO e timer iniciam; 15 rivals têm velocidades 30–34m/s. Pause/results/render capturas parciais não comprovam corrida completa.
- Renderer antes: ~2.685 chamadas/504 geometries; após InstancedMesh de guardrail: ~199 calls/124 geometry, ~189k triangles na primeira amostra. Atualização recente de horizonte levou a ~299 calls/~194k tris (comparação de runtime sob recarga). Hardware/CUDA: Chromium headless `--disable-gpu`; FPS ~20, software acceleration provável, não evidência desktop GPU.
- Identificado erro runtime ao cruzar finish: `resultPosition` estava sem query selector; query foi adicionado, ainda sem completar partida para revalidar results.
- Testes do conjunto após integrações físicas/AI/audio: 67/67 passando; sintaxe do grafo ESM (game.js + Three/CDN + módulos) passa.
- Crítica C15 anterior foi estática/servidor off; screenshot interna real atual base `lunaracer-interlagos-ready-current.png` desktop 1440×900 e captura antiga gameplay `lunaracer-topdown-active-current.png` anterior a versão atual; capture mismatch depois de writers.
- Bloqueadores ainda não resolvidos e marcados: track é estrela polar/genérica, não setores reais (nomes declarados porém não usados); água fora do AABB local mas corredor completo ainda requer geometrical sweep; outer ground strip fecha só edges e grandes gaps ficam backdrop azul; terrain Y/colliders só nearest sample (projection top-down may pass under/over track); hills distant/out-of-frustum estética; spawn grid lateral 4.3m aparente sem pairwise SAT/capture; rivals ainda clusters e collision contact reported; spectator cars can still stack vertically until visual capture; cumulative progress/checkpoint multipliers/end timer/bonus need race end testing; live result/prior lap etc unverified; Gamepad device absent/physical calibration pending; graphics settings changed DPR/shadows but no real multi-profile A/B; no audio/gamelog QA; performance headless only.
- `__THREE_GAME_DIAGNOSTICS__` e hooks instalados; `setState` runtime altera dom/game, mas CDP serialization retornou undefined em tentativas; Playwright inspector ainda precisa confirmar ack, script bug/result finish hook not real finish; hook `results` artificially writes values and no isolated bot test may count as completion evidence.
- Current visual HUD screenshot changed Navy/Cyan after merge; base shot from reviewer `lunaracer-hud-desktop-final.png` predates new track/grid integration, no QA review of actual latest screenshot. Keep gate blocked.
- C08/AI post implementation: nova tarefa iniciada/read-only reviewers após writes (result still pending); C04 post implementation pending; C15/C09 latest HUD pending; C00/game integration pending. Não declarar approval.

FINDINGS CRÍTICOS DA RETOMADA — MODELOS E EVIDÊNCIA
- C08 independent runner `ses_f2d0dcb10ffeu5s2PfN1ycSnVd`: com 3 lanes bloqueadas, AI cai a 0.053 m/s após 10s; deteção caller circular ignora frente/trás; target offset ±0.46 cai targetLane=0; sinal de steering do lateral faz x=2.4→5.241m em 1s; scaling assume 1/60 mas caller trabalha 1/120. `race-ai.test.mjs` passa 15/15 mas não cobre carro físico.
- C04 `ses_f2d0dcabfffeVTAKArqZdHafnA`: `vehicle-physics.mjs` não lê `config.surfaceHeight` e y=2 permanece y=2 quando surface height é12; 8/8 testes básicos passam, ainda sem teste de altura/ramp.
- C15 static read-only `ses_f2d3c394effeoQiyqxnJNNG6C5`: HTTP/CDP não ativos na ocasião; captures guardadas de estado ready/menu (não active play); conclui mesma colisão/isuperfície concerns mas runtime atual não confirmado; não existe aprovação visual atual.
- Auto-throttle root `Math.max(throttle,0.58)` está **removido no código atual em `game.js` na última inspeção**, input neutro alimenta zero. Active neutral playtest ainda precisa executar para comprovante.
- Grid de duas colunas foi aumentado para fileiras 11m e lanes ±2.4m (track width 17); OBB SAT + height interval existe em `game.js`, mas não confirmado com imagens/30s bot.
- Track spline recente pretende ~4,7km polyline Interlagos; curvature/arc length/mínimo/lap flags/checkpoint balance precisam medir após smoothing. Lago está em 72m lateral e 18m abaixo só de um sample; intersection sweep no circuito precisa rodar.
- Correspondente ao C08: grid/start target/gap AI serão retestados após Agent AI retornar. C04 surfaceHeight integrado só depois de retorno. Não declarar esses requisitos completos nesse check.
- Debug server/CDP deve usar screenshot recente/current-run; sem responder debug screenshot velha por baseline atual.
- Correção de rumo: no runtime novo, top-down ficou implementada apenas na câmera `ELEVADA`; a câmera principal permanece chase. A imagem top-down escolhida pode ser requisito apenas do HUD, mas usuário perguntou “mais parecido”; layout actual screenshot é navy/cyan, porém world scene/camera continua low-poly chase.
- Revisão recente de loop foi reiniciada por `__THREE_GAME_TEST_HOOKS__` `setState("ready")` ack resolvido via HTTP/CDP e `setState("active")` → screenshot current, mas hook retorna Promise `{state:...}`; em CDP sem await serialização algumas expressões devolveram undefined. Hook state active deliberadamente zera/facilita corridas e não pode provar countdown real/fail/retry sozinho.
- Countdown real, neutro→GO→input W com profile fresh: timer avança 58→56, corrida racing; jogabilidade W atingiu 34.8m/s em um run e moveu `totalDistanceM=138` em outro; player rank 01 no início/16 ao final em outra execução; colisões com rivals persistem. Capturas de run old screen em `%TEMP%\opencode\lunaracer-after-instancing-active.png` e `lunaracer-active-fresh-after-recovery.png`; screenshot de runway antes de terreno material `%TEMP%\opencode\lunaracer-ground-winding.png` (solo verde escuro presente, sem água cobrindo pavimento naquele ponto de vista).
- Auto-steering assist removido do player neste ciclo por relato “puxando para o lado”; só `inputSnapshot.steer` afeta física do carro do usuário. Needs verify neutral track follows current physics without sideways pull and A/D direction checks.
- New spline card Interlagos mede ~4.955m via diagnostics, sem cruzamento do polyline star curve verificado apenas em um sample technique; old historical trace user accepted 4.740m, mas atual corrigido mais perto 4.3–5km range sem reduzir semântica. Checkpoint count now9, each bonus 15s validated e logic tests 67/67.
- Mudança de superfície lateral: substituir grass global Infield/ground patch winding revisado; renderer profile fresh teve 337 calls/399810 triangles and background remains flat blue sky + green grass, no realistic assets. Screenshot run after patches actual viewport needs collect and compare new reference.
- Water: lago segue plano 88×210 por centerline t=.62 e clearance lateral halfWidth + runoff + halfWidth +8 = ~?? atual código usa 72 m (não é yet sweep); test geometric AABB against all segments required and critical. Mountains moved to `viewGap=260` with y=-10 base; ensure do not occlude road/camera on zoom/top-down.

SPRINT MAX PARALELO — slots efetivamente lançados 2026-09-24
- CONTRATO: cinco atividades independentes, não editam `game.js`; somente implementador HUD edita `index.html/styles.css`, AI-DEV `race-ai.mjs/race-ai.test.mjs`, PHYS-DEV `vehicle-physics.mjs/vehicle-physics.test.mjs`, dois Critics sem escrita. mySystem é único escritor `game.js`, fixtures/final QA.
- Motivo de arquivos novos: nenhum; candidatos anteriores de TrackPath/car model adiados para não fragmentar mais. Manter implementação de cena/world no arquivo já existente.
- AI-DEV completou: `race-ai.mjs/test`, +3 casos regressivos, **18/18**; crítica antes `ses_f2d0dcb10ffeu5s2PfN1ycSnVd` trouxe sinais/lanes/dt/deadlock; uma análise sample confirmou all-blocked policy e caller gap ainda pendente.
- PHYS-DEV completou: `vehicle-physics.mjs/test`, API callback `surfaceHeight(x,z)` com resposta vertical suave a 1/120, **12/12**; crítica antes `ses_f2d0dcabfffeVTAKArqZdHafnA` comprovou y anterior congelava; contato Y integrado em game.js callback TrackPath, precisa validar em slope réel.
- HUD-DEV retomado: `index.html/styles.css`, sem escrita game.js; tema navy/cyan + camada CSS consolidada (~1982→1353 linhas), 44 IDs sem referência ausente, portrait 320/390/landscape 844 sem overflow, mínimo secundário >=12px, foco/reduced motion. Confere screenshot HUD selecionada. Crítica C09/C15 independente ainda pendente na screenshot **atual de racing**; não aprovar só pelo site estático.
- CRIT-GAME original `ses_f2d3c3980ffe9hAC21kp1gLqYv`: provas antes do current cycle: track self-intersection/horizontal mismatch, lake crossing asfalto, camera/altitude/Interlagos scale, colliders não full width e 404/renderer port disabled não reproduzida. Reavaliar `game.js` updated.
- CRIT-VISUAL C15 original `ses_f2d3c394effeoQiyqxnJNNG6C5`: servidor/CDP offline àquela hora; validava lakes/montanhas/altura estaticamente, UI old mobile. Atualmente renderer live process 9227/8765, nova screenshot deverá ser C15 input re-review.
- Limite manager/harness: task model is `webajato/cx/gpt-6-luna`; config individual/effort Max/Ultra por task não está exposto. Set de agentes chamados através da API em paralelo; nenhuma troca/effort claims.
- Minha propriedade: `game.js`, integração/horizons/main camera/physics collisions/state/renderer/hooks; assegurar que nenhum writer toca esse arquivo.
- Gate anterior: testes 67/67 passaram; renderer nova cena antes de solo pair fix tinha 249 calls, state hooked? `ready`; não aprova gameplay sequer.

AUDITORIA INTEGRADA C00/C04/C08/C13 — RUN ATIVO — 2026-09-24
- Task real C00/C04/C08/C13 readonly: `ses_f2c0e3faeffe0hIRgomsyqdBnN`. Status: BLOCK, não aprovar, embora **67/67** unit tests passem.
- CDP gameplay real com Chromium 153/headless + Microsoft Basic Render Driver software, viewport 1440×900; status racing, progress time timer, W input e physics ativos; FPS ~20 é headless software e não prova GPU desktop.
- Idle test detectou carro mudar X/Z ~2,9m parado por contato durante overlap. Checkpoint terminou **+30s** por double-award: `game-logic.mjs:128–139` já soma 15s e `game.js` handler `~1164` soma de novo; ainda não corrigido.
- Colisão atual: car-pair SAT resolve cada par duas vezes (uma vez por ator no loop), tamanho do hero cerca de 5.7m×4.25m vs OBB 4.7m×2.6m, apenas centro Y threshold; no CDP overlap em players e telem collision persists; velocidade relativa é escalar descontada com ratio. Aceite C04 ainda BLOCK até resolução pair canonical, footprint maior e contact cooldown/impulse sem locked slowdown.
- AI caller só usa blockedLanes sem `trafficAhead`; módulos aceitam novas opções mas caller ainda não usa; maneuvers `laneOffset` físicos não são seguidos, steering centraliza sem mirar lane selection. AI policy unit tests não provam 15 rivais de corrida. Aceite AI BLOQUEADO.
- Altitude physics callback `surfaceHeight` agora chega por `game.js`, mas callbacks encontram o track sample mais próximo em 2.048 amostras por veículo/timestep; sem continuidade normal/pneus/segment id, colinas subterrâneas ainda precisam real teste. Rendiment/CPU cost estoutros.
- Lago tem tilt incorreto com sequence rotations Euler: normal local world calculado approx `(-.751,-.660,0)`; orientação esperada horizontal |dot normal up|≥0.99. Lago mesh 88×210, centro calculado próximo do lado da pista; C15 geometric intersection is blocked; deve rotacionar local normal by quaternion or use PlaneGeometry horizontal without yaw or custom buffer vertices, guarantee AABB/track clearance.
- Checkpoints timer double +15s corrigir: uma única layer owner autoriza bônus em exactly 15s.
- C13 medição real modo headless software 1440×900: ~20fps. Renderer current old telemetry por draw/buffers precisa comparar após builds. Vertex scene ~400k tris e ~700 calls em peak antes do instancing; instancing reduziu para ~337 calls mas o CLI C13 atual não prova uma GPU real.
- Camera terceira em game.js top-down atual pode ou não seguir carros; cockpit is obstructed/hero hood likely dominates based on static calculation; needs playtest and screenshot. Terrain/race topology no legitimate confirmation.
- Scope UI cinematic change: screenshot current navy/cyan UI is usable but no image payload stored, cannot evaluate exact color/reference parity; legacy F1 screenshots old state/perspective are not accepted as reference for this selection.
- As referências de cena estão inconsistentes: C07 stage director scorecard requires active play; no AAA/premium claim while blank canvas/context-loss screenshots, auto-error/generic view, no resolved collisions/finish test, no GPU test.
- Binário atual dos anexos de bugs não ficou em workspace, inspeção se baseia em imagem do usuário e nas medidas CDP atuais, não em arquivo externo.

C15/C09 CRÍTICA VISUAL DA HUD — RUN NOVO / UI READ-ONLY
- Task `ses_f2c0e3f9fffeIYqHogVpjFJ6CR`: UI/narrative run game live via HTTP and CDP; desktop 1440×900 and mobile 390×844, click start (actual); NENHUMA aprovação independente da screenshot image top-down porque a referência não está salva em arquivo binário no workspace; old `docs/modelo.png` é arte F1 supersedida.
- UI desktop layout fits but minimap markers ~2.9px; user wants new top-down UI map focal prominent; scaled map remains deformed because x/y projection uses independent scales (`game.js` mapProjection) — scale uniform required; no capture-state comparison to chosen screenshot; C15 remains BLOCK.
- Gameplay test: start/GO→Racing, W moves, ESC pauses/resumes, three cameras cycle; it did not perform full three lap flow.
- Resulting static art decision: do not return to F1-lime HUD; preserve latest navy/cyan HUD color; Interlagos/vehicles/camera-racing gameplay stays.
- HUD reviewer task and network current screenshot showed legibility mobile issue/other values; a local HUD web task may finish static markup/CSS but state data/source changes require C09 re-review.

NOVAS DEMANDAS PRIORITÁRIAS DO USUÁRIO — 2026-09-24 (goal segue ativo)
1. Ajustes: dificuldade e gráficos precisam alterar gameplay/render de verdade; selectors antigos só trocavam texto.
2. 4 cameras distintas corretas: cockpit, chase sobre/atrás do carro, afastada/alta atrás, panorâmica top-down do circuito; modo ELEVADA atual parecia mapa fixo não câmera. Avaliar cockpit bloqueado pelo veículo/carroceria.
3. minimap acompanha progress e posição XY/de heading do player e todos os 15 adversários; screenshot recente marcou jogador fora do mapa e racetrack/mapa sem semelhança. Projection deve ser uniforme, path gerado da mesma TrackPath runtime e markers projetados usando `trackProgress`/sample/lane.
4. Remover objetos não reconhecíveis/ruído em pista: cones geometria não clara que aparecem como impedimentos; set pieces/props nunca à frente/na racing surface; apenas kerbs, runoff/signage reconhecíveis e cones/markers somente em zone de test fora da linha driving.
5. IA competitiva mas vencível, trajetória/speed/lane/ultrapassagem significativas; AI parada ou “louca” reduzida a blockers físicos ou target loops, tem de manter line/circuito e preservar yield/clearance; difficulty efetiva deve modular rival skill/max speed/strategies de forma demonstrada.
6. Carros esportivos dos anexos do usuário substituem monoposto de caixa geometrizada; 9 prints incluem collage de modelos diferentes com lux GT/carros custom e fotos maior detalhe. Mais recente mensagem de usuários antecede referências tower defense; usuário solicita maximum realism. Implementação procedural 3D mais reconhecível (closed coupe sports/hypercars, orientação/pneus/reflexos/paineis, silhuetas/livreiros variadas); assets/Tripo externos só podem ser usados se credenciais reais permitirem; probe oficial informou `TRIPO/GEMINI/ELEVENLABS=MISSING`; não prometer fotorealismo profissional sem modelos/texturas ausentes. Inspecionar a imagem física anexada às mensagens do usuário (não inventar marca/modelo específico; os screenshots não são assets de jogo).
7. seleção de cinco circuitos reais/novos, mapa/geometria distintos, dificuldade vinculada ao circuito mas ainda selecionável. Esclarecer/ou optar: catálogo de circuitos 5 com dados disponíveis; Interlagos mantido e quatro layouts processualmente gerados com identidade, landmarks, checkpoints e length. Nada de selectors no-op. Ao trocar track atualizar TrackPath, ground/mountains, AI, placements, checkpoints, map e start grid; dificuldade muda params de AI e apresentação torna valores reais.
- Preferência visual consolidada: última seleção top-down navy/cyan substitui arte HUD F1; referências F1/sportscar agora guiam modelagem veicular e realismo, pista deve ser Interlagos ou opção selecionada. Essa escolha já foi confirmada pelo usuário; não retornar ao lime F1 HUD.

CRÍTICAS AO BUILD MAIS RECENTE — INTEGRADAS
- C00/C04/C08/C13 live report `ses_f2c0e3faeffe0hIRgomsyqdBnN`, 67/67 unit tests mas QA **BLOCK**; colisão resolve cada par duas vezes, orientação de heading/normal escalar errada, collider 5.7×4.25 visual vs SAT 4.7×2.6, separação persistente repete; idling game move 2.9m em contact. AI caller não manda `trafficAhead/delta`, targeting lane não chega à física; race progress/final checks não executados.
- Defeito de estado race: `game-logic.mjs` bonifica 15000ms e `game.js` adicionava mais 15000ms; timer teste acusa +30s. Corrigido pela retirada do double-add em `game.js`; ainda precisa navegador/C00 validar +15s exclusivamente.
- Lago tem superfície `rotation.x=-π/2; rotation.y=<angleright>` normal medida ≈(-0.751,-0.660,0), NÃO horizontal; bounds cruzam até 3.95m da pista. must fix water mesh horizontal (`rotation x=-pi/2,y=0`) e relocate outside all TrackPath; compute transformed bounds/corridor safe >=20m. Green ground triangles authored as strip but raster winding and gradient/texture viability need check.
- Height callback module+caller existe mas nearestTrackSample O(N=2048) por cada 1/120 physics tick por car -> 15 million operations/s CPU; use track-progress cursor/neighbor sample + bounded local refinement. current callback y `point.y+.18`, suspension no wheel contact/pitch; prove y vs all track ramps and no underpass with screenshots/F2.
- Track polar smooth spline ~4955m sem cross da centerline, porém visual runtime screenshots ainda mostram vistas sem terrain/agua e silhouet Interlagos não evidente; anotação Interlagos sectors declared but never data-driven. C02 previously measured old route cross sectors, new runtime route/star não mapped to actual sectors. `TrackPath` five registry required.
- HUD criteria C09 current minimap rival markers ~2.9px on mobile; uniform x/z scales distort track; new HUD CSS Navy/Cyan preserved; screenshot reference topdown new selection missing local file, C15 requested user-side attachment inaccessible; mark comparison incomplete, not fake.
- Sky/scene latest capture white (earlier run) emitted `CONTEXT_LOST_WEBGL`; recovered fresh-profile screenshot rendered but terrain gaps remain blue/empty. Renderer headless current ~337 calls/400k triangles; one in-screen build had context loss. Must handle context-loss events/recovery, and check memory/resource count across repeated restarts/profile switches.
- Contradictory setting: prior UI agent reduced CSS from 1982 to 1353 lines per note, but latest current stylesheet 1352 lines; difficulty selectors appear in DOM; game.js still toggles camera/difficulty/graphics; need direct verification params change actual config not just label.
- Mouse/user attachments recent: captured screenshot active view showed Interlagos path but vehicles large overlap/static start grid; circled pink on minimap player marker outside track indicates projection/path mismatch; cones circled in front of road are collision props. Correct outputs, not user screenshot annotations as assets.

EXECUTION DISTRIBUÍDA (pedido para o máximo de subagentes; sem alterar modelo/esforço)
- Task tooling supports `general`/`explore`; maximum exposed concurrency natively 5 tasks in most recent batch; no config per child for model/effort. `gpt-6-luna` remains selected session model; effort not exposed. Do not change/infer.
- Slot HUD: independent `index.html/styles.css`, no runtime JS; accepted navy/cyan base state from `ses_f2c0e3fb6ffeg16P6mfzh6KzZZ`; must integrate all five tracks menu, cameras difficulty/quality UI ties only labels unless `game.js` integration consumes.
- Slot AI: independent `race-ai.mjs/race-ai.test.mjs`, has latest `18+` tests; reviewed current implementation. Caller changes game.js only main lead; follow-up C08 review after integration.
- Slot physics: independent `vehicle-physics.mjs/vehicle-physics.test.mjs`, surface height added tests 12+; current code review callback fix. Caller and world state owned by game.js lead.
- Slot 3D asset: `vehicle-models.mjs`, `vehicle-models.test.mjs` new registered candidate; game-ready procedural sports car model factories, unique silhouettes and collision dimensions; no brand fidelity guesses, no API calls. Crit C03 reviewer separate.
- Slot courses: `track-configurations.mjs`, `track-configurations.test.mjs` new registered candidate; five closed paths with signatures and explicit `difficulty`, each no segment crossing, >2km, named; Interlagos as required; tests geometric self intersections/checkpoint order. The world integration game.js remains untouched by writer; C02 rechecks independent.
- 2 concurrent critics: Collision/IA auditor and visual/UI/posture/perf auditor read-only; C00 cross integration after build; slots can be raised through API calls in waves but no write same files.
- Integrator `mySystem` exclusively `game.js`, no other writer while subtasks. Keep screenshot browser and tests focused/resume after worker results, not wait for impossible background continuing after finish.

NOVOS ARQUIVOS CRIADOS PREVIAMENTE AUTORIZADOS NO PLANO (antes dos writers)
- `vehicle-models.mjs` — factory procedural para carros esportivos; evita wall of primitives duplicadas, fornece body bounds/anchors, pintura/materials, layout de rodas; indispensable to pacote disjunto C03 vs game runtime.
- `vehicle-models.test.mjs` — validar count, bounds, local -Z, matching collision footprint, distinct livery state for 16 cars.
- `track-configurations.mjs` — catalogue five versioned course definitions/polylines, sectors, checkpoint progress/difficulty actual param profiles; CPU/math independent world.
- `track-configurations.test.mjs` — ensure each path is closed/no nonadjacent self-intersections/min length/checkpoint order/sector names and limit variation/difficulty profiles.
- Nenhum outro arquivo pode ser criado sem justificação/amend do checklist.

CRITÉRIOS ADICIONAIS DE ACEITE
[ ] cada dos 5 tracks selecionável com shape/minimap/loading/population/checkpoints próprios; trocar cancela/recria world/AI/grid/checkpoint safely; wrap renderer memory counts don't grow.
[ ] cada course bound to difficulty range/params; select difficulty changes actual AI speeds, aggressiveness/gap/yield, max speed; high/medium/low changes actual visuals measured shadowmap/pixelRatio/instancing, not string alone.
[ ] four camera modes: cockpit clear hood/windshield no car geometry filling display; close over-car chase, far chase high, top-down orthographic fits full course and tracks player/rivals. C cycles, pad button cycles; camera never physical coordinate input.
[ ] Minimapa: project track x/z with uniform scale/letterbox, same track length/waypoints. If player sample at point, dot maps to same point within 1.0 unit map basis. 15 markers follow racer physical centers/dots not default start. Test pixel/coords at intersections and ±track bounds. In screenshot player not off-map.
[ ] Specific controls user requested: neutral throttle `0` and stationary body remains `speed<=0.1m/s` for 5sec if no contact; W only after actual held input; no boost/AI assist throttle floor. Coasting naturally decels.
[ ] OBB track/pair overlap deterministic one solve per unordered pair per physics tick; collision pair footprints cover full visible sports car dimensions incl bumpers/swept outline; Y interval surface-aligned prevents under/above-stack; post-resolve penetration <=0.05m; separated bodies do not move under no input; after contact lock release velocity/control recovers <=1sec unless physically obstacle.
[ ] 15 AI use actual selected TrackPath; viewer can overtake, AI passes only if safe (ahead/equal lane gap), no AI advances 0.05 m/s for >3s with route open; AI progress >=0 with loop 30sec, changes lane/score live, won’t instantly converge across grid.
[ ] Collision/props no cones live inside asphalt/racing line; cone/traversability/clearance bounding hit no unlabelled obstacles blocking race path.
[ ] Ground and object vertical Y track-follow on sampled climb/descents; `abs(car chassis lowerBound-groundHeight) <=0.25m`, not underpass/drop; reset uses sampled height; mountains/trees anchored and backdrop bounds don't cross driving corridor.
[ ] Re-run C00 & playtest bot neutral/W/A/D/S/reverse/gamepad mock/camera/selectors track/diff/restart/ref. 3 laps or timeout achieves results. Device hardware pending explicit.

PACOTE C03 — MODELO DE VEÍCULO ESPORTIVO PROCEDURAL — 2026-09-24
Escopo desta execução: criar somente `vehicle-models.mjs` e `vehicle-models.test.mjs`, já registrados na lista de novos arquivos. O usuário solicitou a execução; conforme o gatilho de escrita vigente, criação aguarda a frase explícita “faça sua mágica”.

PLANO
[x] Ler os arquivos Markdown de `docs/` em ordem cronológica, o checklist, os contratos atuais em `game.js` e `vehicle-physics.mjs`, e a disponibilidade de Three.js no runtime local/CDN.
[x] Confirmar que `game.js` já usa `wheels` com `rotation.x` e `steeringWheels` dianteiras com `rotation.y`; preservar esse contrato e orientação local +Y/-Z.
[x] Registrar os dois únicos arquivos permitidos para criação, testes com Three.js real (sem mock) e validação Node solicitada.
[ ] Aguardar o gatilho obrigatório de execução.
[ ] Implementar `createSportsCar(THREE, { paintColor, detailLevel })` com os seis campos de retorno exigidos; geometria procedural de cupê/hipercarro fechado, rodas, detalhes, material PBR e níveis de detalhe.
[ ] Testar silhueta/bounds locais, orientação frontal -Z, dimensões e collider coerentes, geometrias/números finitos válidos e variações de pintura/nível de detalhe usando Three.js real.
[ ] Executar exatamente `node --test vehicle-models.test.mjs` e corrigir falhas do pacote.
[ ] Registrar o resultado do pacote; crítica C03 independente continua pendente até existir uma ferramenta de revisão disponível.

ARQUIVOS A ALTERAR
- NENHUM.

ARQUIVOS A CRIAR (somente após o gatilho)
- `vehicle-models.mjs` — factory procedural injetável de veículo esportivo, contrato/âncoras/dimensões e collider local; arquivo C03 previamente cadastrado.
- `vehicle-models.test.mjs` — testes `node:test` do modelo e contrato com Three.js real, sem implementar Three.js falso; arquivo C03 previamente cadastrado.

ARQUIVOS PRESERVADOS / REMOÇÕES
- Não editar `game.js` nem qualquer arquivo existente.
- Removidos: NENHUM.

RISCOS / IMPACTOS
- Three.js não está instalado localmente. O aplicativo existente usa `three@0.180.0` por CDN e a verificação desta análise confirmou o endpoint acessível; os testes devem carregar a biblioteca real, sem mock, e não introduzir dependência no projeto.
- Geometria procedural permite uma silhueta esportiva reconhecível, mas não substitui modelo/scan ou texturas profissionais. A probe de credenciais registrada acima informa Tripo/Gemini/ElevenLabs ausentes; Tripo e APIs externas não serão usados.
- O módulo será isolado, sem integração em `game.js`, conforme escopo explícito; a integração deve preservar o contrato de propulsão existente e requer pacote posterior autorizado.
- Paleta do modelo: pintura configurável e acabamento PBR navy/cyan; nenhuma alegação de fidelidade fotorealista.
- Harness indica `webajato/cx/gpt-6-luna`; configuração de esforço não está exposta. Não alegar esforço máximo nem revisão C03 concluída.
- Na continuação, o usuário repetiu a autorização explícita (`faça sua mágica`). C03 está em implementação e a espera abaixo não é mais bloqueio.

PERGUNTAS PENDENTES
- Nenhuma pergunta técnica. Falta somente o gatilho explícito “faça sua mágica” para iniciar escrita.

SPRINT MAX DE MICRO-TAREFAS + LOOPS NEO/SMITH — AUTORIZAÇÃO RECEBIDA
- Usuário em 2026-09-24 reiterou literalmente “faça sua mágica e continue”. Loop solicitado: microtarefas paralelas; por cada microtarefa, Neo implementa melhoria, Smith critica resultado, depois correção volta ao Neo até atingir triple-AAA verificável.
- Modelo exposto pelo harness principal: `webajato/cx/gpt-6-luna`; raciocínio/effort Max por subagente não está selecionável nem confirmado. Tarefa/child API oferece `explore/general` mas nenhum campo model/effort. NÃO trocar modelo; NÃO alegar Max/GPT-6 Luna/max configurado individualmente. Personas Neo/Smith rotulam função de tasks reais independentes, mas nunca substituem relato honesto do runtime.
- Paralelismo configurável de slots individuais não exposto pelo harness. Serão iniciados em concorrência todos os pares disjuntos que a ferramenta realmente suportar; lead `mySystem` possui `game.js` exclusivo e uma única verificação integrada.

MICROTAREFAS INDEPENDENTES (domínios e write-sets disjuntos)
- MT01 / veículo esporte. Owner `Neo-Vehicle`: arquivos exclusivos `vehicle-models.mjs`, `vehicle-models.test.mjs`; Smith `Smith-Vehicle` somente leitura, template de modelos/testes/imagem live e dimensões. Aceite: 16 cups fechados, nariz -Z, visuais/bounds/collider coherentes à imagem carro esporte, low/medium/high/livery diferenças; teste browser e scorecard C03 sem claim fotorealista.
- MT02 / competição AI. Owner `Neo-AI`: somente `race-ai.mjs`, `race-ai.test.mjs`; Smith `Smith-AI` somente leitura/runner; Aceite: steer sinais físicos, lane offsets progressivos e driver target real, usar gap dianteiro, difficulty profiles variam, all-blocked gap útil+recovery, volta completa sob TrackPath, sem teleport, state persistent deterministic, 100% testes.
- MT03 / catálogo de corridas. Owner `Neo-Tracks`: somente `track-configurations.mjs`, `track-configurations.test.mjs`; Smith `Smith-Tracks` somente leitura de geometria/selfintersection/sectormap; Aceite: 5 rotas fechadas válidas, Interlagos com setores declarados, distância 4–5 km / restantes identidades próprias, paths/height/checkpoints/difficulty profile, polyline e cubic sampled sem autointerseção; adequar o máximo a levantamento exato disponível, aproximação identificada.
- MT04 / HUD/controlos/menu. Owner `Neo-HUD`: somente `index.html`, `styles.css`; Smith `Smith-HUD` revisão acessibilidade/hierarquia/overflow/state-fit às evidências reais; Aceite: nova screenshot navy/cyan, corrida ainda race, cinco select tracks, 3 difficulties, low/med/high, quatro modos de câmera exibidos/legíveis, map/hud/race overlays e IDs do contrato; 320/390/mobile landscape/desktop, foco/reduced motion/contraste e nenhum dado fictício.
- Cada slot Smith começa em paralelo examinando critérios/código do seu domínio; depois Neo consome findings reproduzíveis concretos e revê os seus arquivos; Smith reavalia somente a correção; loop retorna a Neo se status REPROVADO. A ficha e evidência/score passam só com testes, screenshots e criterios explícitos, não por rótulo triple AAA.
- INTEGRAÇÃO: mySystem-only `game.js`, resultado cada 2–4 diretórios retorna um “import contract” para o lead; alterações dos workers nunca incluem files integrados fora do write-set. O lead edita main loop/vertical track/contact/camera/bot/settings/race bonus; QA executa suíte consolidada depois.
- ACCEPTANCE THREE-AAA POR ITEM: (1) API/behavior exact; (2) runtime progression demonstrada; (3) independent critique sem blocker, retries até aprovação com logs e comparação inicial-final; categoria AAA nenhum placeholder e nota sempre sustentada por evidências/gates, “triple-AAA” pedido é meta da qualidade, não rating numérico inventado.
- Call slots: quando workers completarem write+Smith review, não gastar concorrência em exploração redundante; integrar, reimplementar Neo especificamente para achado e recalcular regression/review do próprio pacote.
- Nenhuma dependência/file novo adicional, nenhuma exclusão. Artif. game-progress already exists. O pacote audio já existente fica apenas sob integração; C12 critica sem alterar.
- Status inicial MT01–04: implementação antiga existente; nova revisão e loops pendentes. Os findings anteriores AI/Physics/HUD/C03/C00 já anotados servem como input; cada Smith verifica branch atual do package, não aprova conclusão anterior automaticamente.

SPRINT MULTIAGENTE — PEDIDO EXPLÍCITO PARA ACELERAR — 2026-09-24
- Usuário solicita máximo de subagentes disponíveis; foram abertas cinco frentes independentes em paralelo: AI writer, physics writer, HUD writer, courses writer e vehicle models writer; dois críticos próprios (physics/game, UI/visual) rodam somente leitura; `game.js` integration fica exclusivo de mySystem.
- No tool harness só existem `general`/`explore`, sem knobs de model/effort por child; sessão corrente model GPT-6 Luna; esforço não configurável. Não ajustar modelo/esforço e não alegar Ultra/Max por worker. As tasks que retornaram IDs e arquivos/testes estão nos relatórios desta conversa; as tasks que vieram como status pre-gate contraditório foram retomadas no mesmo task ID com a autorização literal já presente.
- C10 task `ses_f2aab78b3ffeIdh4o19vJj` estático/CDP antigo reprovou: dificuldade mudava HUD mas não profiles AI; 5 pistas ainda não importadas na revisão; graphics profiles demonstrados em CDP mudaram DPR/shadows e são parcial PASS; checkpoint +30s duplicado; apenas 3 cameras; restart após timeout observado, três voltas browser não. Recheck requerida contra código atual após último merge.
- C03 task `ses_f2aab785bffe68SNWulKYMdhAw` foi pré-model: não havia vehicle-models, carro F1 caixa model. C03 está agora autonomamente criando o módulo `vehicle-models.mjs/.test`; esperar retorno, importar e comparar screenshot nova aos anexos (não available como file).
- C04 latest task `ses_f2b83ad10ffeSu08zgAePLewyb`: P1: 240 directional intersections em vez de 120 pairs; OBB menor modelo (4.7×2.6 vs asa 4.25×car length~5.74; body outer ~2.42/1.18 em patch atual needs compare bounds); velocity response escalar errada frontal; altura usa center delta <1.5 em vez de body vertical interval; constraint border only point, lateral speed continues outside, lastTrackLateral lower actual. C04 espera exact collision redesign.
- C09/C15 task `ses_f2b5b9e78ffeIuohZ2b4KGJkLj`: city style topdown target still differs current chase perspective; 5-pixel font; marker dynamic projection requires same uniform-scale and world coordinate; labels/mobile; course geometry not Interlagos. HUD static changed settings/course IDs only; panel static screenshots old.
- C00/C10/C14 `ses_f2b5b9de8ffepUFc65jbqRuUdV`: difficulty fail (game.difficulty only labels; AI hardcodes skill/speed); five tracks fail despite pure catalog exists; graphics partial pass (DPR cap/shadow map affect real renderer; backend renderer not switched, no FPS evidence); checkpoint bonus +30s (`game-logic.mjs` adds +15 then caller `game.js:1156` adds again); race finish and restart after timeout verified, natural 3 laps not. Track minimap currently world spline coherent only track hardcode; camera array three at time, latest branch declares far fourth but update camera/hud runtime must be tested. Gamepad physical empty, no hardware assertion.
- Producer-specific acceptance: courses pure config has 5 curves tested, but game needs 5 track runtime integration; game.gfx quality changes actual renderer DPR and sun map; difficulty requires profiles alter 15 driver params after reset; checkpoint double addition one owner only; player minimap marker must have correct SVG coordinates and scale, and 4 camera UI cycle confirms exactly four scenes.
- New demo artifacts captured/sourced outside repo need list actual runtime/run-id before any blind comparison. This reference screenshot is not stored as a binary under `docs`; reference old docs/modelo.png is superseded for style. Do not infer visual review from file only.
- **Loop engineering backlog with owner/risk priorities:** P0 `game.js` collision solver/new sports-cars bounds/grid no-stack and neutral hold; P0 AI caller signed front gap + difficulty profile; P0 bonus exactly +15 checkpoint; P1 y surface and ground mesh bounds + lake horizontal/clear; P1 TrackRegistry into view+settings five maps+interlagos route; P1 camera four & topdown mode, minimap uniform; P1 context loss; P2 material/LOD/environment; all items must execute browser and receive separate critical review.

RETOMADA ACELERADA — 2026-09-25

PLANO (gatilho “faça sua mágica” recebido)
[ ] Capturar baseline atual: `node --test *.test.mjs`, verificar servidor HTTP, erros de navegador disponíveis e estado dos arquivos antes de editar.
[ ] Corrigir as falhas reproduzidas na suíte, preservando semântica da velocidade relativa AI e bounds físicos do carro; remover apenas diagnósticos temporários de teste associados ao defeito.
[ ] Auditar colisão SAT, resolução por par único, intervalos verticais, física de contato, altura e integração de tráfego/pistas/câmeras; editar a integração somente para achados reproduzíveis.
[ ] Executar loops independentes com arquivos segregados quando o harness suportar; revisão crítica independente sobre código integrado, sem afirmar aprovações não recebidas.
[ ] Reexecutar suíte completa e validações sintáticas; testar no navegador por HTTP quando Chromium/CDP estiver disponível; registrar falhas e limites sem mascarar.
[ ] Atualizar estado do sprint e relatório final neste checklist e registrar resumo operacional na memória existente em `docs/`.

Arquivos a alterar
- `checklist.txt` — este plano, checkpoints e relatório auditável.
- `race-ai.mjs` e `race-ai.test.mjs` — correção mínima para duas falhas reproduzidas, preservando direção documentada de `relativeSpeedMps`.
- `vehicle-models.mjs` e `vehicle-models.test.mjs` — corrigir envelope/bounds com balanço; remover logs de debug deixados no teste.
- `game.js` — somente se a auditoria de contato, alinhamento vertical, tráfego ou câmera confirmar defeito reproduzível; manter integração centralizada.
- `artifacts/game-progress.md` — atualizar blockers e resultado do sprint.
- `docs/memoria-01a0cd9c-ee61-7b83-a82f-aacd8ce47d64.md` — registrar solicitação e resumo operacional verificável nesta retomada; o identificador/chatid atual não está exposto, portanto não inventar novo nome de memória.

Arquivos a criar
- NENHUM.

Arquivos a remover
- NENHUM.

Riscos/impactos
- Baseline atual falha: 92/95 passaram; reprova tráfego circular/velocidade relativa, ultrapassagem sem sobreposição e bounds verticais do carro com sway.
- Servidor HTTP `127.0.0.1:8765` responde 200; endpoint CDP `9233` indisponível neste diagnóstico. Browser QA dependerá de Chromium/CDP acessível sem gerar novo servidor/processo fora dos existentes.
- Three.js CDN real foi carregado nos testes até aqui; manter os testes sem mocks nem dependências novas.
- A meta de jogo inteiro continua bloqueada por ausência de playtest natural de três voltas e revisão visual atual independente. Mudanças modulares não serão declaradas como aceite integrado.
- Skill `impeccable` não encontrou `PRODUCT.md`; não bloqueia correção de jogo existente. Contexto do skill Three.js Director lido; runtime host não expõe configuração Max por worker.

Perguntas pendentes
- Nenhuma pergunta de escopo. Chat ID atual indisponível no harness; registrar o resumo na memória operacional existente em vez de inventar um ID.

EXECUÇÃO
[x] Ler arquivos `docs/*.md` cronologicamente, checklist, continuidade, instruções de agentes e skill Director; nenhum diretório ignorado inspecionado.
[x] Reexecutar baseline completa: `node --test *.test.mjs` — 92/95 aprovados; falhas identificadas com traces em `race-ai.test.mjs:299`, `race-ai.test.mjs:380`, `vehicle-models.test.mjs:136`.
[x] Confirmar `http://127.0.0.1:8765/` HTTP 200; CDP `http://127.0.0.1:9233/json` indisponível.
[ ] Corrigir AI e bounds model.
[ ] Auditar/integrar correções runtime reproduzíveis.
[ ] Reexecutar todas as verificações afetadas e atualizar relatório final.

RELATÓRIO FINAL
- Arquivos criados: NENHUM.
- Arquivos alterados: PENDENTE.
- Arquivos removidos: NENHUM.
- Resumo por arquivo: PENDENTE.
- Validação/verificação: `node --test *.test.mjs`; sintaxe JavaScript/module graph; runtime via HTTP/CDP quando disponível.

AUDITORIAS DE BUILD ATUAL — 2026-09-24
- C03/C15 writer-review task `ses_f29e74571ffeD1zOMqevsLl8Q5` somente leitura: **BLOCK**. `game.js:710–721` sports branch returns root but does not `scene.add(sports.root)`; old fallback adds scene at line 858 only; factory car thus não é attached/visível após importar. Bounds a static reported 4.84×2.22×1.54m; wheel rim groups. Critic escreveu before `scene.add(sports.root)` patch C03 finding; screenshot active next pass required.
- C03 humanoid/static bounds metrics low/med/high: 22/39/44 meshes and boxes approx L4.76–4.79m,W2.248m,H1.514–1.52m; collider 4.84×2.22 hence wheels extend +1.4cm each side; acceptable small mismatch yet front/rear orientation needs in-engine validation. Procedural poly materials not equivalent scanned car; provider credentials missing.
- C07/C09 task `ses_f29e74...` at previous code revision: four cameras array intended; cockpit incorrectly offset world +Z so camera doesn't follow turning car; far chase follow distinct; elevated panorama is orthographic fixed-centered track, not chase view; no dynamic transition. Latest HUD returns labels 4. Must retest after latest patches.
- C10/C00 audit `ses_f2aab78bfffeIdh4o19vJj`: course count runtime missing, difficulty setting previously only label, graphics select backend not switch (but measurable profile changes shadow/pixelRatio), bonus duplicate +30s previously; patch integrates catalog and one callback owner since then; now test this build again. Natural 3 laps still unvalidated.
- C15 static-only prior review reports not currently valid for changed build. Current `artifacts/game-progress.md` historical baseline mostly before latest code. One latest visual captured under `sports-visible-grid-current.png` shows UI ready with multiple cars but pre/post creation state; active image still lacking current appearance after model attach.
- Evidence fresh runtime on valid HTTP/CDP: `roadLengthM=4955.385`; 5 paths options in DOM, course selector switched successfully to Costa Azul (4027.799m) then Serra Alta (3921.369m), production track labels update; path reload URL query verified. Difficulty profile keys Easy/Medium/Hard actual values max speed 72/81/91mps & skill .5/.7/.89 after starting. Renderer profiles low/medium/high report DPR/shadows/map dimension/exposure; CDP did not sample dpr >1 yet.
- Four camera controls under real input C after race start produced names in order `TRASEIRA PRÓXIMA`, `COCKPIT`, `TRASEIRA LONGE`, `PANORÂMICA`; actual image for all camera modes not reviewed. Topdown marker layout asynchronous? minimap normalized uniform scale and marker cx/cy has correct source patch; verify marker/world pixel.
- Latest test suite 85/85, game.js syntax and esm graph pass as of latest independent round. Does not close C03 integration P0 or C04 collision C08 integration C10 full laps.
- CDP long exercise real: difficulty Hard source calls with selector and AI profile configs available via diagnostics; W changed speed 72km/h plus state racing; player rank switched 16 and collision state available; car/factory render status static initial grid still under audit; screenshots old/out of revision shouldn't pass gate.
- Additional runtime previous image after new factory may show scene but active screenshot `sports-build-active.png` after delayed script shows speed151 rank04 and low detail despite car silhouette still procedural. There are no 16 sports cars attached in one capture proof at present.
- Additional missing API: `game-logic` still natural crossing indices/3 laps; test hook synthetic results path prohibited as natural race evidence. All five track types must each boot/complete mapping and AI race without attachments.

================================================================
PACOTE MT04 — SELETORES DE PISTA/CÂMERA E AJUSTE RESPONSIVO — 2026-09-25
Solicitação: implementar markup e estilos em `index.html`/`styles.css`, preservar direção navy/cyan escolhida e contratos do `game.js`; não modificar lógica JavaScript.

PLANO (antes do gatilho explícito)
[x] Ler todos os arquivos `docs/*.md` em ordem cronológica: `New Prompt.md`, `New Prompt-gemini.md` e memória operacional existente.
[x] Ler este checklist, `AGENTS.md`, `index.html`, `styles.css`, `game.js` e as instruções/padrões de UI aplicáveis; ignorar `.git`, `.vs` e `.idea`.
[x] Conferir referência top-down escolhida: navy/cyan, hierarquia compacta, minimapa destacado; manter jogo de corrida e preservar a área visual da pista.
[x] Inspecionar o contrato DOM do `game.js`: IDs atuais únicos e consumidos, pista configurada por `#track-select`, nome exibido em `#track-name`, câmera runtime default em índice 0 (`TRASEIRA PRÓXIMA`), dificuldade default `medium` e gráficos default `medium`.
[x] Registrar que `#track-select` já existe com os cinco values exigidos (`interlagos`, `costa-azul`, `serra-alta`, `vale-seco`, `porto-ciano`); preservá-lo, não duplicá-lo.
[ ] Após o gatilho, adicionar somente o seletor acessível `#camera-select` no menu/ajustes, com valores `near-chase`, `cockpit`, `far-chase`, `panorama`; primeira opção selecionada para coincidir com a câmera runtime default. A integração comportamental será feita no pacote de jogo, não neste.
[ ] Preservar os seletores atuais de dificuldade (`easy`, `medium`, `hard`) e gráficos (`low`, `medium`, `high`), ambos iniciando em `medium`; manter rótulos de câmera existentes e `#camera-value` para atualização futura da integração.
[ ] Ajustar apenas o CSS necessário: paleta navy/cyan; minimapa destacado; HUD e menus sem encobrir a pista; tamanho legível de pelo menos 12 CSS px inclusive ícones/legenda; ajuste a 320×568, 390×844 e 844×390; safe areas, foco visível e reduced motion.
[ ] Validar estaticamente IDs duplicados/ausentes, valores e defaults dos seletores, ausência de handlers/script inline, tamanhos/estilos responsivos e preservação do contrato DOM; usar Chromium somente se já estiver executando/disponível.
[ ] Registrar resultados no checklist; anotar que os seletores `camera-select`, dificuldade e gráficos são estáticos até integração de `game.js`.

ARQUIVOS A ALTERAR APÓS O GATILHO
- `index.html` — adicionar o seletor de câmera e manter os seletores existentes no menu/ajustes.
- `styles.css` — estilo dos seletores/menus e ajustes responsivos mínimos na direção visual já escolhida.
- `checklist.txt` — rastreio obrigatório, atualização do plano e relatório final.

ARQUIVOS A CRIAR
- NENHUM.

ARQUIVOS A REMOVER
- NENHUM.

LIMITES, IMPACTOS E PERGUNTAS
- A solicitação limita a implementação a `index.html` e `styles.css`; não alterar `game.js`, módulos, arquivos novos ou dependências.
- Não incluir scripts/handlers inline nem inventar estatísticas, modo Tower Defense ou dados de corrida.
- Os cinco tracks já estão ligados à integração atual; conservar seus IDs e values. O novo seletor de câmera será visual/estático até integração posterior.
- Runtime indicado pelo harness: `webajato/cx/gpt-6-luna`; nível de esforço não está exposto, portanto não alegar Max/Ultra.
- Pendência de autorização: a regra operacional requer a frase explícita “faça sua mágica” antes de editar os dois arquivos de implementação. Nenhuma dúvida técnica pendente.

EXECUÇÃO
- [x] Inspeção e planejamento concluídos; somente este checklist atualizado antes do gatilho.
- [ ] Aguardando “faça sua mágica” para implementar e validar `index.html`/`styles.css`.

RELATÓRIO FINAL
Arquivos criados: NENHUM.
Arquivos alterados nesta etapa:
- `checklist.txt` — plano do pacote MT04 e pendência de gatilho registrados.
Arquivos removidos: NENHUM.
Resumo: markup/CSS ainda não editados; `#track-select` foi confirmado existente com os cinco values pedidos; `#camera-select` precisa ser adicionado após autorização.
Validação já feita: leitura estática dos arquivos e requisitos; validação de implementação pendente.
Como validar após execução: verificar IDs/defaults/values e handlers com inspeção estática; testar 320×568, 390×844 e 844×390 no Chromium se disponível.
